# Riverside OS v0.90.0 Certification Evidence

Date: 2026-07-01

## Release Identity

- Release tag: `v0.90.0`
- Source branch: `main`
- Current release target commit: `6064e91cc62a529edea41438a43e085fd99cf8b6`
- GitHub release title: `Riverside OS v0.90.0 — latest build 6064e91`
- Windows deployment asset: `RiversideOS-v0.90.0-6064e91c-Windows-Deployment.zip`
- Release status: source certification passed locally; release asset workflows passed and published refreshed v0.90.0 assets from the replacement tag.

## Source Validation Checklist

Run before tagging and publishing:

- [x] `npm run generate:help` — passed; 68 approved manuals, 0 drafts.
- [x] `npm run check:version` — passed; version parity OK at `0.90.0`.
- [x] `git diff --check` — passed.
- [x] `npm run lint` — passed.
- [x] `npm --prefix client run typecheck` — passed.
- [x] `npm --prefix client run test:e2e -- --workers=1` — passed; 373 passed, 11 skipped on release commit `6064e91c`.
- [x] `cargo fmt --manifest-path server/Cargo.toml -- --check` — passed.
- [x] `cargo check --manifest-path server/Cargo.toml` — passed.
- [x] `cargo clippy --manifest-path server/Cargo.toml --all-targets -- -D warnings` — passed.
- [x] Standalone Tauri cargo checks passed:
  - `cargo check --manifest-path client/src-tauri/Cargo.toml`
  - `cargo check --manifest-path deployment/manager-app/src-tauri/Cargo.toml`
  - `cargo check --manifest-path deployment/server-manager-app/src-tauri/Cargo.toml`
  - `cargo check --manifest-path deployment/counterpoint-bridge-gui/src-tauri/Cargo.toml`
  - `cargo check --manifest-path ros-dev/src-tauri/Cargo.toml`
- [x] Targeted release-critical Rust unit slices passed:
  - `cargo test --manifest-path server/Cargo.toml qbo --lib` — 11 passed.
  - `cargo test --manifest-path server/Cargo.toml counterpoint --lib` — 47 passed.
  - `cargo test --manifest-path server/Cargo.toml backup --lib` — 10 passed.
  - `cargo test --manifest-path server/Cargo.toml restore --lib` — 7 passed.
  - `cargo test --manifest-path server/Cargo.toml helcim --lib` — 27 passed.
  - `cargo test --manifest-path server/Cargo.toml podium --lib` — 6 passed.
  - `cargo test --manifest-path server/Cargo.toml shippo --lib` — 2 passed.
- [x] `npm run check:pre-retag` — passed before moving the replacement `v0.90.0` tag.
- [x] Targeted QBO layaway E2E coverage passed before final retag.

## Release Asset Checklist

Verified after the tag was pushed and release workflows completed:

- [x] GitHub release `v0.90.0` exists: `https://github.com/cpg716/riverside-os/releases/tag/v0.90.0`.
- [x] GitHub release `v0.90.0` is marked Latest.
- [x] Windows POS updater assets exist for v0.90.0: `latest.json`, `riverside-updater-build-manifest.json`, `Riverside.POS_0.90.0_x64_en-US.msi`, and matching `.sig`.
- [x] Windows deployment package exists for v0.90.0: `RiversideOS-v0.90.0-6064e91c-Windows-Deployment.zip`.
- [x] Standalone app assets generated by release workflows use v0.90.0 package metadata for Deployment Manager, Server Manager, and Counterpoint Bridge GUI.
- [x] macOS ROS Dev Center release assets exist for v0.90.0: `ROS.Dev.Center_0.90.0_universal.dmg`, updater manifest, and app archive/signature assets.
- [x] Stale non-current deployment ZIP assets were removed from the v0.90.0 release after publication.

Workflow evidence:

- `Lint Checks` for release commit `6064e91c`: passed (`28489609175`).
- `Playwright E2E` for release commit `6064e91c`: passed (`28489609176`).
- `macOS ROS Dev Center Release`: passed (`28489609908`).
- `Windows deployment package`: passed (`28489609914`).

## Notes

- This document records source-side certification and release-publication evidence only.
- Physical Windows deployment, real payment/shipping/accounting credentials, Counterpoint SQL, and Main Hub restore drills remain external go-live gates.
