# Riverside OS v0.90.0 Certification Evidence

Date: 2026-07-10

## Release Identity

- Release tag: `v0.90.0`
- Source branch: `main`
- Current release target commit: the committed 2026-07-10 release source; the exact SHA is recorded in the GitHub release title, body, and replacement tag.
- GitHub release title: `Riverside OS v0.90.0 - latest build <build-sha>` after retag.
- Windows deployment asset: `RiversideOS-v0.90.0-<build-sha>-Windows-Deployment.zip` after the tag workflow completes.
- Release status: source certification passed locally; release-asset and GitHub-hosted E2E evidence is verified from the replacement-tag workflows before the rerun is reported complete.

## Source Validation Checklist

Run before tagging and publishing:

- [x] `npm run generate:help` — passed; 68 approved manuals, 0 drafts.
- [x] `npm run check:version` — passed; version parity OK at `0.90.0`.
- [x] `git diff --check` — passed.
- [x] `npm run lint` — passed.
- [x] `npm --prefix client run typecheck` — passed.
- [x] Local E2E intentionally not run for this same-version rebuild by operator request. GitHub's tag/push workflows remain the release-run E2E evidence.
- [x] `cargo fmt --manifest-path server/Cargo.toml -- --check` — passed.
- [x] `cargo check --manifest-path server/Cargo.toml` — passed.
- [x] `cargo clippy --manifest-path server/Cargo.toml --all-targets -- -D warnings` — passed.
- [x] Standalone Tauri cargo checks passed:
  - `cargo check --manifest-path client/src-tauri/Cargo.toml`
  - `cargo check --manifest-path deployment/manager-app/src-tauri/Cargo.toml`
  - `cargo check --manifest-path deployment/server-manager-app/src-tauri/Cargo.toml`
  - `cargo check --manifest-path deployment/counterpoint-bridge-gui/src-tauri/Cargo.toml`
  - `cargo check --manifest-path ros-dev/src-tauri/Cargo.toml`
- [x] `bash scripts/cargo-server.sh test --lib` with the configured test database — 463 passed, 0 failed.
- [x] Wedding held-deposit live-database regression — passed through split credit, customer visibility, redemption, QBO liability retention/release, and restoration.
- [x] `npm run check:financial-invariants` — 115 gates passed.
- [x] `npm run check:help-impact` — passed with substantive Register/QBO workflow documentation.
- [x] `npm run check:pre-retag` — passed before moving the replacement `v0.90.0` tag; no local E2E was invoked.

## Release Asset Checklist

The rerun is complete only after these are verified against the 2026-07-10 replacement tag:

- GitHub release `v0.90.0` remains published and marked Latest.
- Windows POS updater assets are replaced for the current build: `latest.json`, `riverside-updater-build-manifest.json`, `Riverside.POS_0.90.0_x64_en-US.msi`, and matching `.sig`.
- The Windows deployment package filename contains the replacement tag's build SHA.
- Standalone Deployment Manager, Server Manager, and Counterpoint Bridge GUI assets use v0.90.0 metadata and current build manifests.
- macOS ROS Dev Center DMG, updater manifest, archive, and signature assets are replaced by the current tag workflow.
- No stale cross-version or old build-specific Windows deployment ZIP remains after publication.

Workflow evidence is the current replacement-tag run set for `Lint Checks`, `Playwright E2E`, `macOS ROS Dev Center Release`, and `Windows deployment package`; the live run URLs and final conclusions are reported with the release handoff.

## Notes

- This document records source-side certification and release-publication evidence only.
- Physical Windows deployment, real payment/shipping/accounting credentials, Counterpoint SQL, and Main Hub restore drills remain external go-live gates.
