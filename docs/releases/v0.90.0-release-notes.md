# Riverside OS v0.90.0 Release Notes

Date: 2026-06-08
Latest same-version rebuild: 2026-07-06
Build SHA: see the GitHub release title and target commit for the latest same-version rebuild.
Release gate: local lint, server checks, financial invariant checks, go-live blocker checks, targeted TypeScript/E2E validation, and production audit probes passed before retag; GitHub release workflows rerun from the replacement `v0.90.0` tag.

## Summary

Riverside OS v0.90.0 is the current source release for GO LIVE readiness. It preserves the v0.85.x POS, deployment, ROSIE, Counterpoint Bridge GUI, customer join/split, and Orders lifecycle baseline, includes the originally published v0.90.0 source cleanup, and adds the latest same-version hardening committed before the rebuilt release assets.

## Latest Same-Version Rebuild Updates

- **Register deposit liability classification and backfill** — classified every real payment made toward open Order, Custom, Wedding, and Layaway transactions before pickup as deposit liability even when staff uses normal **Apply payment** instead of the explicit deposit target. Mixed carts still keep takeaway tender separate, existing open pure-deferred payment allocations were backfilled with `applied_deposit_amount` evidence, and deposit/layaway staff docs now match the real register workflow.
- **Return/exchange zero-credit and Joe Webb repair** — fixed the exchange settlement path so a failed or zero-credit exchange cannot leave a stale return marker that hides the original line from the return wizard. Added a targeted repair for Joe Webb's `TXN-621978` Mantoni shirt return marker and documented balance-due exchange behavior for staff.
- **Bug report download and Tauri file-save repair** — added the missing Tauri file-write permissions, routed bug-report JSON, screenshots, support logs, and browser logs through the desktop-safe file bridge, and changed the Bug Reports panel to report saved/canceled/failed downloads accurately instead of acting like a browser download happened when nothing was written.
- **Z-report display and print feedback repair** — restored Z-report opening during register close and Daily/Register reporting by awaiting the formatted report output path and surfacing clear success/failure toasts when the report cannot open or print through the Reports printer setup.
- **Register #1 station identity and workspace scroll hardening** — improved Station & Network display of local station labels/roles/API source for Tauri registers, reduced false Main Hub/back-office presentation on satellite registers, and added dialog/body scroll-lock handling so Tauri workspace scrolling recovers cleanly after modals and drawers close.
- **Counterpoint stale notification retirement** — retired the recurring Counterpoint stale-sync alert now that Counterpoint sync is a one-time transition operation, resolving existing stale events and removing stale Counterpoint app notifications from Communications & Alerts.
- **Production diagnostics and rate-limit hardening** — tightened customer/transaction diagnostics, ROS Dev Center metadata, notification job behavior, and rate-limit handling around operator workflows touched by the latest register, reporting, and support fixes.
- **App-managed integration credential hardening** — moved the remaining operator-managed integration secrets toward encrypted Settings/App entry instead of `.env`-only management. ROSIE provider credentials, Cloudflare tunnel/API credentials, GrapesJS Studio licensing, Helcim credentials, Metabase credentials, GitHub DevOps tokens, online-store runtime secrets, and related readiness panels now prefer saved app credentials with environment variables retained as deployment/bootstrap fallback only.
- **Metabase OSS Insights integration review and upgrade** — refreshed the open-source Metabase integration against current setup expectations, added the July 2026 integration review artifact, tightened Settings/Insights and ROS Dev Center guidance around Settings-managed admin/staff credentials, and documented the production path for secure embedded/internal reporting without assuming Enterprise-only features.
- **Helcim hosted payments, refunds, and disputes hardening** — kept POS Manual Card on hosted HelcimPay.js, added runtime origin guardrails for Tauri WebView, iPad PWA, and browser sessions, and documented that production HelcimPay.js must run from whitelisted HTTPS checkout origins. POS Payments now includes a standalone Helcim card refund action that records provider attempts in ROS, while Back Office Payments adds an in-app Disputes workbench for duplicate, refund-required, reversal, recovery, and chargeback-like signals.
- **Helcim API and webhook contract refresh** — refreshed Helcim operations docs against the current developer docs, clarified the production webhook URL as `/api/webhooks/card-events`, kept raw card entry outside ROS, and documented the current limit that Helcim dispute case responses remain provider-source-of-truth until Helcim exposes a supported dispute-response API for the account.
- **Root build command alignment** — added a root `npm run build` script that delegates to the existing client production build, matching the root `typecheck` and `lint` aggregator pattern so release validation can run the expected command from the repository root.
- **Staff Admin no-commission attribution account** — added a protected, seeded **Staff Admin** salesperson option for sales and line attribution when no commissioned salesperson should receive credit. Staff Admin is preserved across business-data wipes, hidden from normal staff login and Manager Access approval lists, produces zero commission/SPIFF payouts, and is documented across Register, post-sale correction, staff administration, and commission manuals.
- **Emergency Main Hub update recovery fix** — hardened the Windows Main Hub update path after the July 3 store recovery incident. Main Hub desktop reinstalls no longer preserve stale `station-config.json` API targets, the migration runner repairs legacy wedding-number function ownership before applying pending migrations, ROS startup diagnostics now report Windows task/process/port state when the local API cannot be reached, and Server Manager labels local server health from the actual API response instead of scheduled-task state alone.
- **Donation tender, reporting, and QBO mapping support** — added Donation as a tracked payment tender with required staff notes, receipt/report labeling, QBO mapping visibility, a curated Donation report, and the `reporting.donation_payments` Metabase view so donated items/payments remain auditable by date range, customer, linked transaction, and reason.
- **Pickup payment override and release-blocker repair** — changed partial pickup enforcement so selected merchandise must still be paid for, while remaining-open deposit shortfalls can be released only with Manager Access approval and audit metadata. The rebuild also accepts active commissioned staff for salesperson attribution and removed invalid product-variant active-column predicates that blocked release CI and runtime inventory lookups.
- **Transaction list and receipt release-blocker repair** — fixed the transaction/order list wedding-party SQL grouping that caused 500s in release Playwright lanes, wrapped long thermal receipt header/footer copy for normal-size output, and kept receiptline header/footer text centered without oversized emphasis.
- **Helcim event-to-payment matching repair** — cardTransaction webhooks that already correspond to an existing ROS Helcim payment now link to that payment row with a non-`none` match type, and migration `111_helcim_event_payment_match_backfill.sql` repairs existing event-log rows so Payments Health focuses on genuinely unresolved provider evidence.
- **Helcim manual-card and receipt clarity hardening** — aligned manual card handling with secure Helcim-hosted entry behavior, accepted HelcimPay.js signed callback payloads that preserve the official escaped-JSON shape, and kept ROS receipts as the store/customer receipt source while terminal receipt prompts are handled from Helcim terminal settings.
- **Customer profile live-refresh and ROS-only history labeling** — updated Customer Hub saves to refresh the visible Customer list/current selection immediately after contact edits, and stopped ROS-created customers from being labeled as imported Counterpoint customers in transaction history.
- **Counterpoint direct-import cleanup and order/payment fidelity** — tightened recent direct-import repairs around Counterpoint order duplication, open-document item identity, imported discounts, payment-only activity, and customer/wedding identity matching so ROS keeps the authoritative imported row instead of retaining placeholder or duplicate records.
- **Production financial, discount, commission, and recovery audit hardening** — completed the July 3 production-readiness audit pass across discounts, tax evidence, commission attribution, staff/customer discount identity, QBO/reporting clocks, database probes, job recovery, and operational health checks. Customer preset discounts now follow the selected customer profile before couple-linked financial-owner redirection, staff discounts follow staff-linked customer profiles rather than signed-in cashier identity, Staff pills are visible on staff-linked customer records, post-recognition salesperson corrections remain manager-audited and update commission event snapshots, Daily Financial Report discount totals include POS override metadata, Redis processing jobs recover after stale worker failure, QBO auto-propose and backup schedule checks use the configured store timezone, and the read-only production audit probes now cover discount/customer/employee/commission drift.
- **Receipt layout, split-tender, and Z-report print repair** — normalized Epson receipt header/footer text sizing, changed pickup receipts back to the standard **RECEIPT** title with **PICKED UP** shown in the body, moved pickup order dates to picked-up line detail, made item titles left-aligned and prominent with quantity only when greater than one, printed split tenders as separate **CC/Cash/RMS90/RMS/Check/SC** rows across receipts, Daily Sales, Customer History, and Receipt Builder HTML, and routed Z-close reports through the formatted full-page HTML print path instead of the raw text report fallback.
- **Daily Sales pickup, receipt, and CustomerHub repair** — restored Daily Sales to open on the existing Booked basis while keeping Completed available for financial recognition, fixed Completed session activity so pickup releases count/list correctly, routed payment and pickup receipt actions through the standard Transaction Record receipt layout instead of a bespoke payment slip, and made Daily Sales customer names/Customer # links open CustomerHub for the exact linked customer.
- **Windows package migration checksum compatibility fix** — changed server startup, Main Hub install/update, and standalone migration checks to accept CRLF/LF-only checksum differences while still failing real SQL drift. Windows deployment packaging also preserves the live Main Hub migration checksum history by shipping packaged migrations `001-101` as CRLF and `102+` as LF, with release gates checking the compatibility rule.
- **RMS payment and alteration receipt/reporting clarity** — RMS Charge payment lines now stay explicitly tagged as payment-on-account lines in the Register cart, customer receipt, Daily Sales activity, and printed audit reports instead of appearing as ordinary retail merchandise. Alteration service lines now show as **Alteration** in the cart, receipts, Daily Sales, and printed audit output while remaining separated from normal merchandise revenue in the financial reporting paths.
- **Receipt barcode and universal transaction lookup fix** — receipt barcodes now preserve and scan the real `TXN-*` receipt number, Register lookup exact-matches scanned transaction barcodes, and Universal Search can open the matching Transaction Hub record directly from a scanned receipt barcode.
- **Receipt Builder and customer receipt content refresh** — Receipt Builder multiline header/footer editing now preserves spaces and returns, preview tokens include customer identity, staff, payment, wedding/custom order, SKU, variation, and payment sections, and receipts now show customer full name, phone, and Customer # where attached.
- **Payment-only and order-payment presentation repair** — payment-only activity remains visible in Customer History and Daily Sales as payment activity instead of hidden sale shells, imported Counterpoint payment/tax presentation no longer shows tax as a false overpayment, and payment receipts include customer identity, applied Transaction Record, payment amount, and remaining balance.
- **Inventory stale-zero-stock hiding and search recovery** — products with zero stock and no sales in the last five years can be hidden from normal inventory/search views while remaining recoverable from inventory find with an explicit hidden-item view and reactivation path. Meilisearch documents now carry hidden state so stale hidden products do not pollute normal scan/search results.
- **Counterpoint tax, variation, and barcode cleanup** — added focused repair migrations and import cleanup for Counterpoint open-document tax presentation, variation labels such as split shirt ranges and stray `*` values, and barcode/SKU exact-scan behavior so `B-*`, `I-*`, `CP-*`, and `ROS-*` scans return an exact item or **SKU NOT FOUND**.
- **Wedding import identity and duplicate repair** — added migrations and import hardening for wedding-created customer identity, duplicate customer repairs, phone normalization, and customer-number assignment so wedding party links do not replace normal ROS customer numbers.
- **Wedding import customer-code and receipt identity fix** — stopped Wedding Manager cutover imports from creating `Wedding-xxxxxxxx` customer numbers and routed new wedding-created customers through the normal ROS customer-code sequence. Customer-facing receipts now carry the attached customer’s full name, phone, and Customer # across thermal print, Receipt Builder HTML/email, and SMS fallback output.
- **Register search overlay close fix** — closed stale product-search result panels after item selection, variation selection, scan handling, or focus moving away from the search box so dropdown results no longer cover the transaction line list after staff add or scan an item.
- **Helcim declined-payment retry and approval recovery hardening** — added an explicit **Retry card** path after declined terminal attempts so staff can clear the final decline and send a new ROS-tracked Helcim request for another card, while keeping **Recover payment** available when the terminal actually approved but ROS still shows pending/declined. Helcim webhook and manual recovery now safely attach a later approved same-invoice/same-amount terminal result to the original attempt instead of leaving the decline latched on the checkout screen.
- **Register pickup completion and receipt flow fix** — changed Customer Orders pickup actions so selected pickup lines load into the register cart instead of being fulfilled inside the modal. Staff can add new purchases, complete pickup through the normal register action, and reach the Sale Complete receipt screen for printing/texting/emailing a pickup receipt.
- **Receipt item-detail readability fix** — changed thermal and HTML receipt item rows so the full product name, variation/size description, SKU, quantity, and price remain visible instead of clipping variation details into the price row.
- **Wedding import duplicate-customer prevention and merge repair** — tightened wedding cutover customer creation so seven-digit worksheet phones are no longer treated as verified CRM phone identities or used to auto-match imported customers, and fixed the customer merge service to repoint `transactions` rather than the old `orders` table name. This lets safe exact-name/full-phone duplicate repairs merge imported `ROS-*` wedding placeholders into the authoritative Counterpoint customer records.
- **Wedding party import customer-link hardening** — fixed Wedding Manager cutover imports so worksheet note/instruction rows stay as party notes, worksheet paper-status columns do not auto-complete ROS workflow states, imported sizing is applied to created members, and customer links are auto-verified only when normalized phone plus customer name identify one unique existing customer. Ambiguous phone matches now remain in review instead of silently attaching to the newest customer.
- **Counterpoint payment dedupe and full-history rerun repair** — added an explicit `CP_FORCE_HISTORY_FLOOR` override for AI-led direct go-live repairs and included Counterpoint payment sequence numbers in closed-ticket and Open Docs payment payloads. Payment dedupe now includes the payment sequence, preventing same-tender/same-amount payment rows from collapsing during full-history repair imports.
- **Counterpoint source-authority and SKU scan hardening** — tightened the Counterpoint import contract so full/go-live write imports cannot override failed preflight blockers, Bridge no longer honors the old import-with-blockers bypass, and import runs record the actual preflight pass/fail state. POS SKU-style scans for `B-*`, `I-*`, `CP-*`, and `ROS-*` now behave as exact lookups only: a missing scanned SKU returns **SKU NOT FOUND** instead of falling into similar-number/name search results.
- **Counterpoint barcode alias and matrix integrity fix** — preserved all valid Counterpoint `B-*` barcode aliases for a variant instead of deleting/replacing existing aliases during import cleanup, allowing items with more than one Counterpoint barcode to scan correctly. Catalog preflight now blocks broken matrix parents and matrix cells whose `I-*` family does not match their parent, while valid single non-matrix `I-*` items without a usable `B-*` barcode receive deterministic `CP-*` recovery SKUs without removing Counterpoint-owned data.
- **Counterpoint data-integrity runbook update** — documented Counterpoint SQL as the source of truth for customer profiles, open orders, gift cards, loyalty balances, catalog identity, and inventory quantities, with ROS limited to additive `CP-*` recovery identities when Counterpoint has no usable barcode.
- **Helcim stale payment recovery fix** — expired stale pending Helcim terminal attempts before reporting terminal routing status and allowed staff-authorized recovery to release stale no-provider attempts from closed register sessions, preventing old card attempts from blocking the next credit-card transaction.
- **Final go-live deployment and audit-probe hardening** — corrected the production PostgreSQL 14 deployment guide to use supported WAL/checkpoint settings, and tightened the read-only production audit probes so canceled void-payment audit history and unapproved QBO warning proposals do not appear as active payment or accounting integrity defects during go-live signoff.
- **Direct layaway deposit QBO journal fix** — included direct layaway cash/card deposit inflows in the daily QBO `liability_deposit` journal and drilldown evidence, while preserving deposit release semantics at pickup/fulfillment. This keeps layaway deposit collection visible to accounting on the payment date without recognizing merchandise revenue early.
- **GO-LIVE performance and connectivity hardening** — added the GO-LIVE multi-device performance/connectivity review, mapped the critical Register, Back Office, Wedding Manager, POS search, customer/order lookup, checkout, register close, receiving, and Insights data paths, and hardened staff sign-in/API bootstrap behavior for LAN and Tailscale use. Back Office startup and PIN sign-in calls now have explicit timeouts and staff-facing Main Hub recovery guidance, the checkout offline replay timer is cleaned up on all outcomes, the legacy API helper now respects the shared runtime API base for Tauri/PWA/Tailscale clients, and Register # outage banner recovery has focused Playwright coverage.
- **Station-scoped login and register session hardening** — added server-issued station tokens and a stable station key contract so Back Office, Main Hub, PWA, Register #1, Register #2, and other connected devices cannot reuse another device's POS session or cashier identity. POS session APIs now require the matching session id, session token, and station key for register-owned actions, while the Back Office top bar continues to prioritize the staff member signed into that workstation instead of the register session cashier.
- **Register connectivity and state recovery hardening** — preserved register identity and open-session state through Main Hub/API outages, replaced disruptive reconnect flashing with clearer degraded/recovery states, and expanded focused E2E coverage so Register #1 and related workstations keep their own lane/session context after disconnects and reconnects.
- **Helcim terminal recovery and duplicate-use guardrails** — renamed cashier recovery controls to **Recover payment**, kept approved-but-unrecorded terminal attempts blocking Z-close, prevented the same approved Helcim attempt from being applied or checked out twice, and documented that Riverside lanes use ROS receipts as the store/customer receipt while Helcim terminal receipt printing must be disabled in terminal/device settings.
- **Counterpoint Open Docs duplicate cleanup and pickup unblock** — changed Open Docs import reruns to treat parent-placeholder-only Counterpoint rows as unresolved unless a real matrix/barcode identity is present, skip and delete duplicate Open Doc transactions when the matching fulfilled Counterpoint ticket transaction already exists, and added a targeted Bridge preflight mode so Open Docs repair reruns do not stall on unrelated catalog probes. POS order pickup now refuses already-closed/no-open-line transactions instead of opening the Return/Exchange wizard, and Manager Access pickup overrides verify the selected manager's Access PIN instead of a generic admin role check.
- **Counterpoint closed-ticket balance repair** — changed closed Counterpoint ticket-history imports and customer-facing history projections so historical ticket rows remain fulfilled sale history with zero active balance, while real open documents/orders continue to carry balances. Customer Browse, Customer Hub, and open-work counts now exclude closed Counterpoint ticket refs, and migration `094_counterpoint_closed_ticket_balance_presentation.sql` repairs existing imported rows during Main Hub update.
- **Reports printer direct-dispatch fix** — changed desktop report-table output, including Register #1 Reports printing, to use the configured Reports printer direct-print path instead of forcing the formatted HTML temp-preview opener that Tauri can block on Windows temp files.
- **Customer and Inventory list readability refresh** — increased key text sizes, icon affordances, status badges, stock/balance labels, and row separation in the Customer and Inventory list workspaces while keeping rows compact enough for high-density 27-inch monitor use.
- **Counterpoint inventory key-match import fix** — changed ROS inventory landing so a Counterpoint inventory row with a trusted `counterpoint_item_key` updates the already-linked ROS variant even when the incoming fallback/recovery SKU differs, instead of creating a false item-key conflict and leaving stock rows unlanded. The Bridge also supports a `CP_RECOVERY_SKU_FORMAT=legacy6` compatibility mode for the AI-led direct import rerun so generated `CP-*` recovery SKUs can line up with prior six-digit cleanup runs while the default remains collision-resistant for new runs.
- **Counterpoint direct-import proof repair** — fixed Open Docs extraction/proof to collapse duplicate SQL join rows by stable document, line, and payment keys before posting to ROS, so source proof compares logical open documents against the landed ROS documents instead of tripled SQL join output. Catalog recovery now also scans inventory source rows, allowing a no-barcode Counterpoint matrix cell to create its deterministic `CP-*` fallback variant before inventory lands, and fidelity diagnostics compare catalog money at cents precision to match ROS storage.
- **Main Hub update migration drift fix** — restored the already-applied `092_counterpoint_live_tender_aliases.sql` migration to its original checksum, moved the live `SQUARE` tender mapping into new migration `093_counterpoint_square_tender_alias.sql`, and changed the Windows Main Hub installer so migration checksum drift fails loudly instead of continuing into a server startup failure. The Main Hub update wait loop also reports scheduled-task state and `riverside-server` process count while waiting for port `3000`, making server-start failures visible in the elevated update window.
- **Counterpoint AI-led direct import hardening** — hardened the direct Counterpoint SQL to ROS import path for the fresh go-live rebuild: matrix cells now prefer real `B-XXXXXX` barcodes from Counterpoint `IM_BARCOD` rows, including `*` dimension values, and only generate deterministic collision-resistant `CP-*` recovery SKUs when no usable barcode exists. Counterpoint ticket/open-doc reruns now rebuild existing imported rows instead of skipping stale data, the default go-live history floor is `2018-01-01`, live `SQUARE` tenders map to `credit_card`, imported zero-sale payment artifacts are hidden from normal transaction history, open-order deposit displays use the imported payment allocations, and the new direct-import runbook records the reset/rebuild/fresh-import procedure and live-run evidence.
- **Main Hub in-app update recovery fix** — changed the generated elevated `update-runner.ps1` to avoid PowerShell 5.1-fragile interpolated double-quoted output and non-ASCII text, closing the parser failure that stopped Main Hub updates before `install-server.ps1` could run. The Updates panel now allows a narrow Main Hub recovery path: if the full Main Hub runner fails, staff can check/install the Windows app update first, relaunch Riverside, and then rerun the full Main Hub update with the repaired runner generator.
- **Main Hub update config handoff fix** — changed the in-app Main Hub updater and `install-server.ps1` to find the saved deployment config from the install root, ProgramData, extracted update package, or prior Downloads deployment package, then persist it to the Main Hub install root before repair/register update steps run. This prevents the elevated update runner from stopping at `Config file not found: C:\RiversideOS\riverside-deployment.config.json` after the server is already down.
- **Deployment Manager PostgreSQL credential save fix** — moved PostgreSQL Admin Password entry into the main Database Health panel, added explicit **Save & Check** behavior, and kept the Updates tab password field from acting like a silent unsaved edit. Database Health, migrations, repair, reset, and Main Hub update actions now share the same saved deployment config password instead of failing because the credential was hidden or not persisted.
- **Counterpoint catalog import live-flush restore** — reverted the catalog importer away from the buffered post-after-SQL-completes path and back to the earlier live batch flushing behavior so catalog batches begin landing in ROS while SQL rows are still streaming instead of stalling at the first large post-extraction catalog handoff.
- **Counterpoint generated SKU format and review cleanup** — changed generated Counterpoint recovery SKUs to deterministic `CP-*` values, keeps parent numeric fallback rows compact, uses collision-resistant matrix-cell values across batches, and changed generated SKU review rows to report-only rows with no repair action buttons. Landing issue rows no longer show Bridge/rerun/recheck/source-copy actions from ROS; unsupported actions were removed instead of pretending to fix landed proof.
- **Remote Access Cloudflare Tunnel in-app provisioning** — moved the store Cloudflare Tunnel setup into ROS Settings so operators can save the public callback host and Cloudflare API credentials through the encrypted credentials card, then run **Repair Cloudflare Tunnel** to create/configure the tunnel, route DNS, install/restart `cloudflared`, and verify the live callback path from the app instead of editing local tunnel files or running setup commands manually. The Windows installer also clarifies the ROSIE `.env` handoff and can repair/create local tunnel config from configured deployment values when needed.
- **Main Hub readiness timeout fix** — bounded `/api/ready` dependency probes so database or Redis readiness problems return quickly instead of hanging the installer/updater behind repeated HTTP operation timeouts after `/api/health` already proves the server process is alive.
- **Counterpoint ticket-history cutover anchor fix** — moved the required Counterpoint history floor to January 1, 2018 for go-live imports, and fixed the Bridge so full/go-live/fix reruns use that fixed floor consistently while only **Update Since Last Run** uses saved cursor dates. Ticket child queries now share the parent ticket anchor, preventing recent ticket headers from being compared against full-history ticket lines/payments.
- **Counterpoint live SQL routeability review** — probed the live Counterpoint SQL source for catalog, ticket, open-doc, customer, gift-card, and tender-code routeability; added live Riverside tender aliases for `RMS 90 DAY`, `STORE CRED`, `LOYALTY`, `DONATION`, and `PROM GC`; and removed the Bridge payment fallback that could label blank/unknown Counterpoint tender codes as cash instead of surfacing them as unmapped exceptions.
- **Counterpoint catalog stream stall hardening** — changed the Bridge catalog watchdog so a large Counterpoint catalog query can pause mid-stream without aborting the import after the first 180 seconds. The Bridge now keeps the shorter first-row timeout, allows a longer default stream timeout after rows start flowing, and logs continued SQL waiting instead of failing a live import while SQL Server is still processing.
- **Counterpoint catalog buffered-ingest rollback** — the buffered post-after-SQL-completes catalog path was removed after live store imports stalled at the catalog handoff; the Bridge again flushes catalog batches to ROS during SQL streaming.
- **Counterpoint catalog extraction correctness fix** — changed Bridge-generated parent catalog SQL to aggregate price, inventory-cost, and barcode joins to one row per Counterpoint item, preventing one product from expanding into hundreds of duplicate parent rows. Catalog extraction now completes and dedupes before catalog batches are posted to ROS, and ROS ignores partial landed proof from failed import runs so failed extraction cannot leave a misleading half-catalog go-live proof state.
- **Counterpoint ticket-history preflight hardening** — changed Ticket History proof so a tiny `PS_TKT_HIST` header count with huge line/payment counts is treated as a header/detail query-alignment blocker instead of a passable warning, and changed the Bridge GUI console so compatibility warnings render as warnings rather than red errors.
- **Counterpoint generated-SKU and Open Docs recovery hardening** — changed catalog and inventory imports so Counterpoint rows with valid item identities but blank, invalid, duplicated, or missing historical cell barcodes receive deterministic `CP-*` SKUs instead of being discarded solely for bad barcode data, records generated-SKU import exceptions with the original source payload for report-only review, and keeps active Open Docs strict by requiring unresolved items to be fixed before sign-off instead of importing placeholder/fallback obligations.
- **Counterpoint custom-order SKU and proof hardening** — protected Riverside OS reserved custom-order SKUs `100`, `105`, `110`, and `200` from Counterpoint catalog takeover, added a migration to restore the seeded custom-order products while preserving Counterpoint copies as `CP-*` SKUs, improved Inventory/Register search ranking for parent product matches such as `40901`, and tightened the Counterpoint direct-import workflow, reset guidance, and release package checks from the latest go-live fixes.
- **Counterpoint Bridge update action** — added an explicit **Update Since Last Run** control to the Counterpoint Bridge GUI so operators can run the supported `incremental_update` mode from the app instead of relying on a hidden API parameter.
- **Counterpoint live-import run-kind/schema fix** — removed the old rehearsal mode as the active Counterpoint import contract, made full Bridge imports record as `full_import`, added a migration that converts legacy `rehearsal` / `full_rehearsal` rows and updates the `counterpoint_import_runs` database constraint, and added a go-live gate so Main Hub schema, Bridge payloads, and server normalization stay aligned before release assets rebuild.
- **Counterpoint unresolved-item review fix** — removed the historical fallback product/resolver path from sales history and open-order imports. Counterpoint lines that carry an item identity must now match the imported ROS catalog/inventory first; if they do not, ROS records a needs-review exception with guidance to fix the active ROS review issue when supported, map only true aliases/duplicates, or delete the issue when it should not block sign-off.
- **Fresh install custom-order migration fix** — made the custom-order SKU restore migration self-contained by seeding its `Custom Orders` category before restoring the ROS custom-order products, preventing fresh GitHub Actions, installer, and reset databases from failing with a missing category foreign key.
- **Wedding data auditability and protection review** — added the comprehensive data integrity, protection, security, and auditability review artifact; tightened wedding destructive workflows so member removal, appointment deletion, and checklist-only non-inventory item removal now commit only with a matching `wedding_activity_log` entry; and updated staff guidance so removals are treated as audited operational changes rather than silent cleanup.
- **Inventory tag printing and vendor catalog identifier hardening** — tightened Inventory List and Product Hub tag printing so direct Zebra tag dispatch does not silently fall back to preview, prefilled tag quantities from on-hand stock, added per-variation quick tag printing, and expanded Add Item/Product Hub/Quick Add receiving flows with manufacturer Product UPC plus main product and SKU-level Catalog # / vendor style # identifiers. Purchase Orders, printed/emailed PO output, and Receive Stock now show the effective vendor catalog number for each line, using SKU-level overrides such as `40411-B` when present and otherwise falling back to the main product catalog number such as `40411`, while receiving scan matching avoids auto-selecting ambiguous shared catalog numbers.
- **Helcim refund and Payments operations hardening** — audited all Helcim API usage against the current Payment API, Hardware API, webhook, batch, settlement, refund, reverse, and idempotency docs; documented the endpoint map and remaining physical-terminal gates in `docs/audits/HELCIM_API_USAGE_AUDIT_2026-06-21.md`; kept terminal hardware purchases/refunds pending after `202 Accepted`; required customer/original-card-present confirmation for debit terminal refunds; added a first-class **Helcim backend refund** register record path with Manager Access and mandatory Helcim refund reference; and verified the Payments workspace is wired to Helcim transactions, batches, deposits, reconciliation, terminal health, webhook replay, and recovery actions.
- **Fundamentals review and reports/intelligence validation fixes** — added the 11-section fundamentals review artifact in `docs/audits/FUNDAMENTALS_REVIEW_AND_FIX_PASS_2026-06-21.md`, tightened the Reports catalog stale-subtitle check so dynamic Reports subtitles are recognized correctly, and hardened the Intelligence + Finance E2E spec to use the real local API base and fail missing prerequisites instead of silently skipping core checks.
- **Release asset consolidation and obsolete Counterpoint payload removal** — consolidated Windows automatic release publishing into `.github/workflows/windows-deployment-package.yml`, where the Main Hub deployment ZIP, Windows POS updater assets, Deployment Manager, Server Manager, and Counterpoint Bridge GUI updater assets are built and verified together. The macOS ROS Dev Center workflow remains active because that app is the Mac-side management companion. Retired Counterpoint package-preparation scripts, Windows launchers, token helpers, and duplicate Bridge-only release workflow output were removed, and same-version release reruns now delete stale deployment ZIPs and old hotfix ZIPs that cannot be replaced by `--clobber` without deleting current macOS management assets.
- **Windows deployment release publish hardening** — hardened the Windows deployment workflow so it cleans stale package output before assembly, requires exactly one current `RiversideOS-v0.90.0-<build-sha>-Windows-Deployment.zip`, uploads only that ZIP to the GitHub release, logs the selected package and size before upload, and times out the release publish step instead of hanging indefinitely if GitHub release upload stalls.
- **Faster Windows updater release paths** — added explicit `app-updater-only` and `main-hub-update` scopes to the Windows deployment workflow. `app-updater-only` publishes the signed Tauri updater assets for Register and Back Office desktop app updates. `main-hub-update` publishes a focused `RiversideOS-v0.90.0-<build-sha>-MainHub-Update.zip` for the in-app Main Hub update path, including server/API, client files, migrations/seeds, ROSIE runtime assets, and the local desktop app while skipping Deployment Manager, Server Manager, and Counterpoint Bridge GUI rebuilds. Full tag releases still build the complete deployment ZIP and companion tools.
- **Main Hub updater build matching fix** — changed the in-app Main Hub updater to prefer the focused `MainHub-Update.zip` asset when present, keep accepting the full Windows Deployment ZIP for full release packages, and tolerate existing 7-character versus 8-character build asset names while still refusing the wrong build.
- **Fresh database reset path** — changed the packaged `Reset-RiversideDatabase.cmd` production wrapper to reset ROS to a clean migrated database with only required seed data, with an explicit confirmation prompt before removing store/import data.
- **Release build caching optimization** — added shared Rust/Tauri compiler caching with `sccache` on the Windows deployment and macOS ROS Dev Center release workflows, while keeping the existing npm, WiX, and Rust dependency caches. This is intended to reduce repeated same-version rebuild time without removing any updater or deployment artifacts.
- **Counterpoint direct ROS go-live simplification** — removed the old package handoff from the operator path and removed the Bridge engine's old fallback path. Counterpoint go-live now has one normal route: the Bridge connects to Counterpoint SQL, posts source proof and raw batches directly to Main Hub ROS, and ROS tracks current-run proof, exceptions, customer duplicates, and final sign-off.
- **Counterpoint import workflow clarity cleanup** — removed confusing old package language from the current Counterpoint go-live screens, renamed the main tabs to **Import & Proof**, **Customer Duplicates**, and **Support Diagnostics**, and kept operators on the direct path: run Bridge import, review ROS proof, resolve exceptions, review duplicates, then sign off.
- **Counterpoint sent-vs-landed proof clarity** — changed the Bridge GUI from extraction language to **Import to ROS** language and added ROS proof guidance that separates rows the Bridge sent from rows ROS landed, so operators can see when connection/import succeeded but remaining exceptions still block go-live sign-off.
- **Main Hub updater and Counterpoint Bridge SQL paste fix** — aligned Windows deployment ZIP naming with the Main Hub updater's 8-character build matching so in-app updates can find the exact release package, hardened the release gate against 7-character deployment assets, and made the Counterpoint Bridge accept pasted `.env`-style `SQL_CONNECTION_STRING="..."` values by saving the normalized raw SQL string with explicit Server/Database validation instead of launching a broken extraction run.
- **Counterpoint Bridge SQL configuration startup fix** — hardened the standalone Counterpoint Bridge GUI so saved SQL and Main Hub ROS settings are passed directly into the bridge engine at launch, invalid/empty SQL connection strings fail with explicit operator guidance, SQL connection failures stop extraction/preflight instead of cascading into closed-connection and missing-mapping noise, and the header shows **SQL Attention** instead of a misleading connected state when only the local dashboard is reachable.
- **Refund, deposit, QBO, and print audit hardening** — made card refund provider approvals durable and retry-safe so approved Helcim refunds are not re-sent after local ledger failures; added wedding group-pay open-deposit source traceability through deposit ledger/QBO drilldown; blocked QBO approval/sync when gift-card tender classification is missing or unknown instead of falling back to generic tender mapping; and aligned Staff Access / Manager Access / Access PIN copy plus tag-print feedback so EPL/ZPL and PWA preview outcomes are stated accurately.
- **Counterpoint Bridge health fix** — changed the Bridge GUI to validate the dedicated `/api/bridge/health` JSON contract, so a Vite/static page or wrong local service can no longer masquerade as the Bridge API. The Bridge direct-import path targets Main Hub ROS for raw Counterpoint extraction handoff.
- **Tag Builder full-name and print parity fix** — changed builder-driven EPL2 tags to wrap full product names across the saved product-name box instead of truncating them, aligned preview text wrapping with the real print path, and added regression coverage proving long names survive the EPL2 renderer.
- **PostgreSQL backup execution correction** — kept production backups on PostgreSQL `pg_dump`, added `RIVERSIDE_PG_DUMP_PATH` support for Windows installations where PostgreSQL client tools are installed outside `PATH`, and restricted Docker backup fallback to explicit non-strict-production opt-in only.
- **Counterpoint Bridge operator simplification** — simplified the Bridge GUI setup to the one-time migration path: Counterpoint SQL connection plus the Main Hub ROS URL. The normal Bridge screen uses no-cache JSON health checks before extraction and no longer routes operators through a separate package-preparation app.
- **Windows deployment package cleanup** — removed unused in-ZIP updater asset duplication and the full `release-docs` docs mirror from the Windows deployment package, kept signed updater manifests/installers as GitHub release assets, made the Main Hub installer install the curated package docs instead, and hardened ROSIE model download retries for wrapped HTTP 429 rate-limit responses during package assembly.
- **Counterpoint production-scale import proof** — verified Bridge extraction against the real Counterpoint SQL database and proved the direct Main Hub ROS intake can receive and proof staff, sales reps, categories, vendors, catalog, vendor items, inventory, customers, customer notes, tickets/sales movement, open docs/orders, store credit opening balances, loyalty balances, and gift cards before production-hardware sign-off.
- **Counterpoint ROS preflight test isolation** — hardened backend Counterpoint health tests so saved Back Office credentials are isolated during test cases, keeping package preflight, import proof, duplicate-email handling, and health behavior covered without depending on local machine credential state.
- **Counterpoint Main Hub LAN reachability fix** — made the Bridge GUI derive the Main Hub ROS URL from the LAN address instead of falling back to `127.0.0.1`, added wrong-machine/non-JSON health diagnostics, and updated setup docs so Bridge PCs use `http://<Main-Hub-LAN-IP>:3000`.
- **Tag Builder text-size and price-fit controls** — added per-field text size controls for every text/number field in the Tag Builder, preserved barcode sizing through Width/Height, and made EPL2 tag printing auto-shrink text inside each builder field so large sale prices such as `$119.00` do not print into the barcode area.
- **Station & Network Main Hub truthfulness fix** — separated the current browser API host from the Main Hub LAN API, shows `Main Hub` when the local server is detected, and avoids presenting missing installer metadata as a broken API when the Main Hub API is reachable.
- **Tag Builder regular/sale layout split** — replaced the old preset tag layout path with one active builder system, added separate saved Regular Tag and Sale Tag layouts, and made sale tags expose regular price, sale price, and savings as independently movable fields while test and real EPL2 prints use the same saved builder positions.
- **Counterpoint reachability proof** — added Bridge GUI health checks before extraction starts and improved retry/error reporting so reachability failures name the exact Main Hub ROS URL.
- **Counterpoint local handoff simplification** — made the normal closed-store Bridge → ROS handoff require only the SQL connection string and Main Hub ROS URL; simplified Back Office and Bridge GUI setup screens, env examples, simulator, and docs accordingly.
- **LP 2844 tag layout preservation and barcode clearance** — restored all Tag Designer layouts and configured tag fields, restored barcode captions/footer output, changed test tags to a representative `B-XXXXXX` barcode sample, and added small-tag barcode clearance so prices do not print inside barcode bands.
- **Counterpoint Bridge extractor target correction** — made Bridge manual full/targeted extraction use the same direct Main Hub ROS raw-batch handoff as scheduled extraction, fixed the manual trigger `pool is not defined` runtime error, and kept `ros_import_first` as the one-time go-live target mode.
- **Zebra tag layout fit hardening** — bounded EPL2 text, price, footer, and barcode placement to the saved physical tag size, disabled printer-generated barcode captions that could add hidden height, added regression coverage for one print command per tag and in-bounds layout coordinates, and added an LP 2844 retail tag size preset/warning in Tag Designer to prevent saved tall layouts from feeding extra blank stock on standard Riverside tags.
- **Register checkout recovery hardening** — stopped online server `5xx` checkout failures from silently falling into the offline queue, added manager-visible checkout recovery blockers for unconfirmed online checkouts and paid pickups that fail to complete fulfillment, broadened Register/Top Bar closeout messaging beyond “offline sale,” and surfaced post-commit checkout support follow-up failures as recovery alerts instead of log-only events.
- **Counterpoint Bridge Windows package cleanup** — removed the retired standalone `counterpoint-sync-workbench`, `Start-CounterpointSYNCWorkbench.*`, and bridge-token helper payloads from the Windows deployment ZIP. Counterpoint go-live now uses the Bridge GUI under `counterpoint-bridge-gui` to post directly to Main Hub ROS.
- **Counterpoint Import Command Center alignment** — refocused the ROS Back Office Counterpoint screen on direct Bridge import proof, exceptions, customer duplicates, and the required business-area paths for customers, inventory, ticket history/sales movement, open orders, gift cards, and loyalty points.
- **Counterpoint baseline reset FK guard** — hardened Counterpoint baseline reset so preserved ROS transaction lines that still point at imported Counterpoint products/variants are kept, detached from those stale catalog references, and no longer block reset with product variant foreign-key errors.
- **Counterpoint direct-import architecture** — made the preferred handoff Counterpoint Bridge → ROS Import Command Center, preserving ROS as the final validator/importer into PostgreSQL through the existing Counterpoint import pipeline.
- **ROS Counterpoint Import Command Center truthfulness** — expanded Back Office Counterpoint status surfaces to separate Bridge heartbeat, SYNC reachability, browser/control reachability, selected run/section readiness, ROS preflight/import state, callback mismatch warnings, run-scoped final proof, and clearly demoted support diagnostics.
- **Counterpoint Bridge direct-target mode** — kept Main Hub ROS destination URL configuration, Bridge extraction batch handoff to ROS, simulator-friendly setup docs, and direct ROS compatibility checks behind explicit configuration.
- **ROSIE read-only semantic data layer expansion** — added audited, permission-gated live-data tools for wedding readiness, customer follow-up, stale pickups, vendor/PO/receiving exceptions, gift card/store credit liability summaries, QBO/register summaries, data cleanup tasks, and manager attention queues while keeping ROSIE unable to run arbitrary SQL or mutate Riverside OS.
- **ROSIE live-data safety metadata and routing** — added fail-closed audit behavior for all live-data read tools, serialized tool metadata for input schema/sensitivity/audit policy/mutation allowance, conservative natural-language routing for clear operator questions, and mutation-like prompt suppression for fix/adjust/reconcile/post/receive/refund/fulfill requests.
- **ROSIE approved-tool planner** — replaced fragile keyword-first live-data routing with a structured approved-tool planner that classifies business domain, selects only typed read-only tools, validates required arguments, refuses mutation requests, logs safe unsupported tool gaps, and returns candidate customer/wedding/vendor matches before sensitive answers instead of guessing.
- **ROSIE Chat clarity and live-data answer hardening** — simplified the chat surface, hid source chips from normal ROSIE Chat, forced conversation-mode requests to concise citation-free responses, shortened spoken replies, and made approved report/read-tool payloads answer directly instead of letting the model claim accessible sales, pickup, inventory, customer, or operational data is unavailable.
- **ROSIE natural-language data guardrails** — expanded best-selling, open-order, and ready-for-pickup intent routing, added deterministic answers for structured report/read-tool results, routed broad "do we have" questions by business domain before inventory, and made unmatched data-shaped questions ask for the missing customer, item/SKU, date range, or workflow detail instead of guessing.
- **Staff schedule request-off and appointment identity hardening** — added staff-submitted request-off/time-away records, manager review/approval flow, request-linked effective exceptions, absence/call-out handling, stable appointment `salesperson_staff_id` identity, and audited manager overrides/reassignments so appointments, tasks, and published schedules all use the same staff schedule source of truth.
- **Staff birthday in-app notifications** — added optional staff birthday month/day fields, validated Feb. 29 handling with Feb. 28 observation in non-leap years, in-app-only birthday notifications for active staff scheduled to work that store-local day, and a once-per-staff/day login greeting popup after successful Staff Access / Access PIN login.
- **Main Hub report-printer argument fix** — changed the Tauri desktop and Main Hub print-server report paths to run their Windows print script via a temporary `.ps1` file with `-File`, fixing Reports printer names with spaces such as **Admin Office Printer** that previously caused PowerShell to treat the printer name as a command and fail with an empty content path.
- **Riverside LP 2844 tag-printer path** — kept the production tag-printer language on EPL2, restored use of the saved Tag Station target, and treats **Zebra LP 2844** as the expected Windows queue rather than a hidden forced runtime name.
- **Main Hub startup and local API hardening** — made the Windows installer readiness checks use `/api/health` and `/api/ready`, preserve real server diagnostics, force the production runtime mode over stale machine-level `RIVERSIDE_MODE=development`, and exempt trusted loopback desktop traffic from the global API rate limiter so the Main Hub does not throttle its own app calls.
- **Customer entry layout stability** — moved duplicate-customer review below the active identity/contact fields and constrained long match lists so the Add Customer drawer no longer shifts while staff type.
- **Emergency Tauri printing hardening** — routed clothing tags through the configured Riverside **Zebra LP 2844** EPL2 path, kept receipts on the receipt station, added a real Tag Test Print path, bounded native TCP print writes, preserved receipt retry recovery after completed sales, and added targeted E2E coverage for printer settings plus tag payload generation.
- **Production tag-printer routing fix** — made Tag Designer and existing inventory tag print actions use the saved Tag Station EPL2 route, block missing tag configuration with clear errors, and avoid preview/success fallbacks when direct tag dispatch did not happen.
- **Counterpoint import proof hardening** — required live bridge runs to stay on the import-first path, made batch proof create and count run-scoped exceptions for unlanded/quarantined source rows, and kept duplicate customer email rows landing without the conflicting email plus actionable exceptions.
- **Counterpoint Bridge tokenless go-live path** — the trusted store-LAN Bridge workflow no longer requires a Counterpoint sync token for Main Hub ROS intake.
- **Counterpoint health/status token alignment** — updated Counterpoint command-center status, bridge health, heartbeat/preflight/import validation, and troubleshooting docs so a token saved in the Main Hub is treated as the canonical production token instead of requiring staff to edit server `.env` by hand.
- **Zebra tag station proof-path fix** — changed Printers & Scanners **Print test tag** to send a real inventory tag through the same saved EPL2 route used by inventory workflows.
- **Tag print success wording correction** — changed tag print feedback from “accepted” to “sent to the saved Tag Station using EPL2” so a Windows spooler handoff is not overstated as physical printer proof.
- **Deployment production-mode correction** — separated Deployment Manager `environmentMode=production` from strict-production enforcement so production installs no longer write `RIVERSIDE_MODE=development` by default while still preserving explicit strict-production opt-in behavior.
- **ROS Server Manager health false-negative fix** — added top-level local API readiness routes and made Server Manager locate/read the installed Main Hub server `.env`, reducing false **API Offline** / **PostgreSQL not reachable** states caused by missing config discovery instead of actual service failure.
- **Main Hub installer database-host regression fix** — locked Deployment Manager Main Hub install/update config writes to local PostgreSQL host `127.0.0.1`, repaired stale URL/label-shaped database hosts in `install-server.ps1`, and changed the credential precheck to use the safe PostgreSQL reachability helper so DNS/socket failures no longer terminate the installer before repair can run.
- **Main Hub in-app updater exact-build fix** — changed Settings → Updates on the Main Hub to pass the latest build SHA into the native updater, select the matching Windows deployment ZIP, verify `deployment-package.manifest.json` before launching elevated PowerShell, and report the step as runner launch rather than falsely claiming the server finished updating.
- **Counterpoint Bridge connection visibility fix** — restored a first-class Main Hub connection status block showing token state, accepted heartbeat, bridge host, phase, and version so operators can distinguish a locally listening Bridge from one ROS is actually accepting.
- **Loyalty reward eligibility floor fix** — restored the Elite Rewards rule that only customers with at least 5,000 points appear as ready for a reward gift card, repaired existing stored thresholds below 5,000, and blocked future settings saves below the reward floor.
- **Receiving/QBO financial invariant hardening** — corrected receiving, QBO inventory movement, and register-close inventory valuation so supplier inbound freight is never capitalized into merchandise item cost, kept customer shipping separate from supplier freight, clarified receiving report labels, and added a release-gated financial invariant check covering freight, shipping, gift cards, deposits, discounts, QBO staging, and wedding-program economics.
- **Deployment Manager execution/logging hardening** — unified Install and Update execution output into the same full-size console, persisted Deployment Manager and Server Manager action logs, added direct log-open actions, removed dead seed controls, replaced browser confirmations with in-app confirmations, and added static release gates proving visible actions map to packaged Windows scripts.
- **Windows installer rerun recovery** — changed migration-ledger recovery and bootstrap-admin setup to update-then-insert instead of relying on uniqueness constraints that may not exist yet on existing Main Hub databases, preventing repeat install/update attempts from failing before migrations can repair the schema.
- **Deployment Manager configuration and script prompt cleanup** — fixed standalone/Main Hub config writes so register API base and station labels are saved correctly, made Counterpoint Bridge token sync collect the token in the GUI instead of hanging on a hidden PowerShell prompt, and masked sensitive script arguments in persistent logs.
- **Tauri printing configuration cleanup** — removed stale printer endpoint fields from Register Settings, kept receipt auto-print there, and routed staff to Printers & Scanners as the single source of truth for Receipt, Tag, and Reports printer targets so Tauri Reports and Zebra tag printing read the same station settings staff configure.
- **Tauri print audit proof** — revalidated Reports, Tags, and Receipts routing through the native Tauri bridge: Reports use the installed Reports printer command, tags use the configured Tag Station EPL2 route, receipts remain on the receipt station, and the responsive Reports print affordance E2E passed across phone, tablet, iPad, and desktop.
- **Counterpoint v8.4 ticket preflight repair** — fixed the real Bridge blocker where a low `PS_TKT_HIST` header count stopped import even though `PS_TKT_HIST_LIN` and `PS_TKT_HIST_PMT` proved substantial ticket history, and hardened generated ticket SQL to prefer `BUS_DAT`, use composite ticket identity, and join ticket lines/payments/notes on all visible ticket identity columns.
- **Counterpoint import-first go-live flow** — replaced the operator-facing staged wizard success path with a source-count preflight, explicit import-run lifecycle, proofed batch import, landed-row provenance, visible exceptions, and the go-live readiness command center.
- **Pre-retag release guardrail** — added `npm run check:pre-retag`, a guarded `npm run release:retag -- v0.90.0` wrapper, and GitHub workflow pre-build enforcement so version parity, deployment gates, go-live blockers, print routing, dirty existing-database migration rehearsal, server migration tests, client typecheck, lint, and whitespace checks pass before rebuilt assets are requested.
- **Main Hub update migration repair** — repaired installer, standalone migration, local Docker, and server startup migration paths so stale PostgreSQL serial sequences are advanced before pending migrations apply, preventing existing databases from failing on duplicate primary keys during the v0.90.0 update.
- **Unified print routing hardening** — centralized Reports, receipt/cash-drawer, and tag print dispatch through shared print services, kept Reports output on the configured Reports printer, kept receipt output on the receipt station, and made tag preview fallback stop marking variants shelf-labeled unless the direct Zebra route confirms the job.
- **Counterpoint source-count blockers** — made suspiciously low ticket/open-doc counts, wrong ROS base URL, missing sync token, empty required SQL mappings, and wrong history floor fail loudly before import so a run with 39 open docs or 57 tickets cannot masquerade as complete.
- **Counterpoint raw/provenance proof ledger** — added import-run, source-count, raw-record, provenance, and exception tables so every supported imported ROS row can be traced to Counterpoint source entity/key/hash and extracted timestamp.
- **Counterpoint Bridge import-run wiring** — changed the Bridge to start a ROS import run after preflight, post import-first batches through the proofed endpoint, record failures as import exceptions, and complete/fail the run explicitly.
- **Fresh baseline reset hardening** — extended reset to clear imported Counterpoint rows, import-run proof, exceptions, staging state, and active import-run state while preserving migrated schema, seed/admin access, store settings, register/printer configuration, and reviewed mappings.
- **Counterpoint command center validation** — added backend import-run proof coverage, refreshed the settings UI test coverage, and documented the simplified one-time migration workflow for operators.
- **Counterpoint real-data live-import hardening** — blocked Counterpoint Step 1 advancement when Bridge-reported rows have no applied or ROS landed proof, and added a read-only real-data test run evidence pack covering Bridge ingestion, POS/register smoke, inventory movement, and QBO sandbox safety.
- **Fresh E2E migration runner fix** — changed the Docker migration runner to record early migration ledger rows without relying on `ON CONFLICT` before the ledger primary key is created, restoring clean isolated Playwright database startup.
- **Main Hub updater runner parser fix** — removed the fragile PowerShell format expression from the generated `update-runner.ps1` wait loop and expanded the deployment release gate to parse the fully rendered runner script before release assets build.
- **Register report direct-print fix** — changed POS Z-Report, Daily Sales, and report-table output in the desktop app to send printable audit text through the configured Reports printer bridge instead of the desktop HTML preview path.
- **Counterpoint/print/update regression gates** — expanded the go-live blocker gate to prove Register Reports use the native Reports printer route, Counterpoint Bridge GUI query entities still map to engine SQL keys, Main Hub Sync shows staging counts before live apply, and Counterpoint Bridge GUI updater assets remain wired into Windows release packaging.
- **Counterpoint staged-sync visibility fix** — changed the Main Hub Counterpoint Step 1 table to count queued/applied staging batches by entity so rows successfully sent by the Bridge no longer appear as false **No Data** warnings while Safe Staging is on.
- **Counterpoint bridge request completion fix** — aligned the Bridge request-completion endpoint with the ROS route and kept compatibility for the previous path so successful manual sync requests no longer leave stale `SYNCING`/pending state after batches post.
- **Main Hub bridge controls reachability fix** — made the Counterpoint Bridge control API bind to the workstation network by default and made Main Hub try the saved URL, heartbeat hostname, `127.0.0.1`, and `localhost` before reporting direct controls unreachable.
- **Counterpoint SQL Query Tester alignment** — mapped Bridge GUI workflow entities such as Staff, Sales Reps, Vendors, Store Credits, Vendor Items, Receiving, and Orders/Tickets to their runtime SQL keys so diagnostic tests no longer report false `Unknown query entity` errors while the sync engine is extracting successfully.
- **Counterpoint tester SQL limiting fix** — corrected read-only SQL preview limiting for `SELECT DISTINCT` queries so Categories and other distinct projections compile as valid SQL Server `SELECT DISTINCT TOP 10` probes.
- **Counterpoint Bridge GUI startup hardening** — stopped the standalone Countersync GUI from auto-starting the Node bridge engine on window open and kept settings load/save on the lightweight app-data config path, reducing the launch freeze/not-responding window.
- **Counterpoint workbench quarantine count correction** — changed the Main Hub Inventory Catalog badge from raw accumulated quarantine audit rows to distinct current catalog/inventory identities and relabeled it as **Unique Quarantined** so repeated failed/replayed imports do not show impossible multi-million item counts.
- **Desktop print/preview blocker clearance** — fixed the Tauri desktop `open_path` ACL used by Tag Designer, Help Center, and HTML print-preview surfaces, removed remaining component-level browser print bypasses, and made blocked/failed print previews surface staff-visible errors instead of success toasts.
- **Zebra LP 2844 tag printing fix** — kept tag output on EPL2 for the Riverside LP 2844 tag printer, switched Tag Designer preview to the real Tauri desktop detector, and made Tag Designer's **Print test tag** action send a real sample label.
- **Tag Designer installed-Zebra language fix** — **Print test tag** now resolves the saved Tag Station queue and sends EPL2 instead of reporting a successful generic Zebra spool job with no physical label.
- **Main Hub print server tag dispatch** — added a station-based `/api/hardware/print-station` path so inventory tags route through the Main Hub print server to the saved Tag Station target, with client-side tag actions no longer relying on each Tauri shell's local raw printer command.
- **Direct station printer preservation** — kept receipt, tag, and Reports printer dispatch on the existing `ros.hardware.printer.*` station settings while routing only HTML preview/fallback surfaces through the desktop-safe preview helper.
- **Print route coverage gate** — added a classified print-routing manifest and `npm run check:print-routing` so pre-build checks fail if a new print call appears without direct/preview routing proof or if a receipt, cash drawer, tag, or Reports route drifts to the wrong bridge.
- **Counterpoint bridge sync blocker clearance** — scoped the generic API rate limiter away from token-bearing Counterpoint bridge ingest routes so full high-volume inventory/customer sync batches are not cut off with `ROS 429`, while preserving existing bridge sync-token validation.
- **Counterpoint workbench and packaged Help hardening** — fixed the grouped data-source health SQL that could 500 the Counterpoint workbench and embedded approved Help manual markdown into the server binary so packaged Windows installs do not depend on dev-repo `client/src/assets/docs` paths.
- **Go-live blocker pre-rebuild gate** — added `npm run check:go-live-blockers` to fail before an expensive rebuild if known Print, Counterpoint, workbench SQL, Tauri ACL, or packaged Help regressions return.
- **Release workflow pre-build gate expansion** — extended the go-live blocker gate to cover current release notes, generated Help coverage, Windows ROSIE process-lock guards, release workflow gate wiring, and same-commit Playwright gating for the Windows updater release before expensive asset builds begin.
- **Counterpoint v8.4 bridge runtime mapping** — rebuilt the Counterpoint bridge around Riverside's NCR Counterpoint POS v8.4 schema probe flow so normal setup keeps `.env` to `ROS_BASE_URL`, `COUNTERPOINT_BRIDGE_TARGET_MODE=ros_import_first`, and `SQL_CONNECTION_STRING`, while stale `CP_*_QUERY` values are ignored unless `CP_SQL_ENV_OVERRIDES=1` is explicitly enabled for expert recovery.
- **Counterpoint SQL proof tooling** — added a read-only `node index.mjs sql-smoke` command that compiles every available runtime SQL mapping against the live Counterpoint company database without printing row data or posting to ROS.
- **Counterpoint Bridge GUI connection-only save** — changed the standalone Countersync GUI save path so it rewrites only the three supported connection settings and no longer preserves or reintroduces old entity SQL in `.env`.
- **Counterpoint live-schema validation** — verified the live Riverside Counterpoint SQL Server mapping for customers, loyalty balances, store credit, customer notes, catalog, matrix cells, inventory, vendors, vendor items, gift cards, tickets, payments, ticket notes, and open docs before publishing this rebuild.
- **Repository cleanup and Counterpoint bridge consolidation** — removed the duplicate tracked `counterpoint-bridge 2` tree, kept `counterpoint-bridge/` as canonical, and moved stale root-level docs, historical reviews, media, reports, and planning artifacts into structured archive locations.
- **Sidebar icon uniqueness fix** — separated Customer Notifications from Podium Inbox in POS/sidebar navigation so distinct app functions no longer share the same rail icon.
- **POS/report print failure feedback** — routed Backoffice update and Z-report/Daily Sales print failures through the app toast/diagnostics path instead of blocking browser alerts.
- **Counterpoint Bridge GUI clippy cleanup** — resolved strict clippy failures in the standalone Counterpoint Bridge GUI Tauri crate so workspace clippy can pass with warnings denied.
- **Windows release workflow hardening** — added retry handling around Windows Tauri updater bundling and enabled Tauri's local tool cache so transient WiX/NSIS download outages do not fail release reruns after compilation succeeds.
- **Order Detail audit and pickup workflow cleanup** — simplified Order Detail into one clear Order Progress view and one Pickup/Shipping Check, removed duplicate status cards and technical labels, changed pickup release wording to staff-friendly item language, and wired the footer pickup action to the actual pickup release modal instead of opening Register.
- **Custom Order review/edit from Order Detail** — reused the POS Custom Order form in edit mode so HSM and Individualized Shirt details can be reviewed and corrected from Order Detail, with server-side custom SKU validation and canonical detail persistence.
- **Orders list and detail language cleanup** — replaced `NTBO`, line/unit terminology, and old lifecycle/status wording with staff-readable labels such as Ready to Order, Needs Staff Check, items, and price.
- **Product Hub vendor and non-stock SKU cleanup** — replaced the secondary vendor checkbox wall with searchable secondary vendor selection and stopped presenting Internal/POS and Custom SKUs as stock-counted on-hand inventory.
- **Print feedback hardening** — added user-visible failure feedback for Orders/Reports print window failures so print actions no longer silently do nothing when the browser or workstation blocks print setup.
- **ROSIE local knowledge retrieval** — added ROSIE's approved local knowledge index over current Help manuals, staff docs, and policy docs so Help/ROSIE answers no longer depend on Meilisearch-only Help search availability.
- **Metabase Insights launch hardening** — fixed automatic Insights launch to use saved encrypted Metabase Admin/Staff credentials before env fallbacks, aligned Settings and ROS Dev Center readiness checks with that behavior, and added a non-destructive Metabase verification mode for reporting schema/sample database/shared-auth checks.
- **Bug report screenshot capture hardening** — improved browser screenshot capture/print handling around unsupported CSS color functions so bug reports can keep working across browser and Tauri contexts.
- **Commission spiff and combo workflow cleanup** — redesigned SPIFF setup around scanner-friendly SKU/product targeting, product-based combo requirements, staff bonus totals, and commission reporting alignment for fulfilled/pickup/takeaway/completed transactions.
- **Windows ROSIE deployment asset fix** — updated bundled Kokoro/espeak TTS support-file paths so the Windows deployment package can assemble the ROSIE stack assets without failing on stale upstream Hugging Face paths.
- **Inventory promotion workflow cleanup** — improved the Promotions management layout, added clearer active/scheduled/closed controls, enabled full-inventory promo scope, moved performance detail into a report-style workflow, and improved scanner-focused SKU entry for selected-SKU promotions.
- **Inventory reporting and physical count polish** — added Inventory Reports sidebar discoverability, refined Physical Inventory wording/readability, and aligned staff-facing inventory docs with the current workflows.
- **Daily sales weather reporting** — added a curated **Daily Sales Weather** report, surfaced captured weather in Daily Sales/Register Day Summary, and exposed `reporting.daily_sales_weather` for Metabase dashboards.
- **Ready-to-test blocker clearance** — fixed Wedding Manager group-pay member attribution, fixed wedding financial-context decoding after group pay, and removed an invalid `transaction_lines.updated_at` write from alteration-ready propagation.
- **Ready-to-test audit evidence** — added the v0.90.0 READY TO TEST blocker audit with a GO FOR READY-TO-TEST recommendation, targeted validation evidence, and the manual store test-run checklist.
- **Physical Inventory hardening** — completed the Physical Inventory workflow hardening and receiving-flow updates, including lockout/validation coverage around inventory counts and receiving behavior.
- **POS pricing and release workflow hardening** — tightened POS pricing behavior and release workflow proof paths for the same-version rebuild process.
- **Employee discount checkout fix** — restored employee discount application in POS so eligible staff/customer discount handling flows through checkout correctly.
- **RMS Charge customer matching** — improved RMS Charge customer matching to reduce mismatches during account/payment lookup.
- **POS cart usability fixes** — compacted the POS cart staff header, contained register cart scrolling, and removed the passive Staff Access / Manager Access toggle from persistent POS chrome so sensitive actions request Manager Access at the action point.
- **Staff task tracking** — improved staff task tracking surfaces for operational follow-up and store readiness work.
- **QBO and pickup E2E hardening** — strengthened QBO audit assertions for persistent E2E data and aligned pickup certification fixtures with the real Manager Access readiness guardrails.
- **Daily Cash Deposit at Z-close** — added deposit date and amount capture to register close, persisted those fields in register session history, and printed them on Z-reports for accounting review.
- **Desktop-safe report/export handling** — routed CSV exports, Help printing, loyalty letters/labels, order reports, inventory tag previews, bug-report downloads, and Daily Sales/Z-report previews through Tauri-safe desktop file/open flows instead of browser-only pop-up/download paths.
- **Deployment Manager install status hardening** — made the install wizard show role-specific live output, block Finish until the selected install succeeds, and keep the Main Hub install sequence tied to the explicit wizard execution path.
- **Deployment release rerun gate** — added a Windows deployment release check that validates deployment copy, generated updater PowerShell syntax, Deployment Manager launch logging, and deployment PowerShell parse health before the release package workflow proceeds.

## What Changed

- **QBO explicit mapping enforcement** — journal staging no longer uses a generic fallback account. Any exportable financial activity must have an explicit Chart of Accounts mapping before it can be posted.
- **QBO inventory adjustment key cleanup** — the legacy inventory adjustment revenue key was renamed to `REVENUE_INVENTORY_ADJUSTMENT` so the mapping matrix remains clear and auditable.
- **Pre-go-live local review fixes** — locally verifiable QBO, Counterpoint, backup/restore, Helcim, Podium, Shippo, and release/update code paths were reviewed, with targeted fixes applied where possible.
- **Helcim and Shippo test hardening** — health-check and credential-state tests now avoid nondeterministic local/CI behavior while still covering disabled, missing-token, and healthy states.
- **Docs/help refresh** — active deployment guides, manuals, Help Center sources, release notes, and certification evidence now point to v0.90.0 as the current release.
- **Release metadata bump** — root, client, server, Tauri, standalone tools, ROS Dev Center, and Windows deployment metadata are aligned at `0.90.0`.

## Release Assets

The v0.90.0 GitHub release is published as Latest and the release workflows populated the required assets.

Published assets include:

- Windows POS updater manifest: `latest.json`
- Windows POS build manifest: `riverside-updater-build-manifest.json`
- Windows POS MSI and matching `.sig`
- Windows deployment package: `RiversideOS-v0.90.0-6064e91c-Windows-Deployment.zip`
- Standalone app packages for Deployment Manager, Server Manager, and Counterpoint Bridge GUI where produced by the Windows deployment workflow
- macOS ROS Dev Center DMG and updater assets where produced by the macOS workflow

## Validation

Source validation and release-asset validation are tracked in [`v0.90.0-certification.md`](v0.90.0-certification.md). The latest same-version blocker audit is recorded in [`../audits/v0.90.0_READY_TO_TEST_BLOCKER_AUDIT.md`](../audits/v0.90.0_READY_TO_TEST_BLOCKER_AUDIT.md).

## Remaining External Gates

These cannot be completed from source alone and must still be verified on real deployment targets:

- Full Windows Main Hub install/update/uninstall rehearsal
- ROSIE install/update test on target machines
- Counterpoint SQL live sync reconciliation
- QBO sandbox post/void/re-stage proof
- Helcim, Podium, Shippo, and QuickBooks real credential workflow tests
- Backup restore rehearsal on the Windows Main Hub
