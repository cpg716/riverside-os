# Riverside OS v0.95.0 Certification Evidence

- Release tag: [`v0.95.0`](https://github.com/cpg716/riverside-os/releases/tag/v0.95.0)
- Published replacement commit: `b954edadaec2a872e3529343567af01fa71da7a6`
- Publication verification: **2026-07-24 17:35 UTC**
- GitHub release state: **Latest, not draft, not prerelease, and targeted to the
  full replacement commit**
- Pre-rerun public build: `f0c42ddc62295325dce6aaad3997f2f85a25f634`
- Operational status: **Source, guarded local non-E2E validation, exact-commit
  GitHub Lint/Playwright, signed Windows/macOS builds, and published-asset
  identity are verified. Physical production installation and live-system
  signoff remain separate.**

## 2026-07-24 replacement scope

The replacement preserves the existing sale-isolation and provider-recovery
guards and adds four focused corrections:

- Counterpoint-priced returns, refunds, voids, and exchanges fail closed for 540
  reviewed ambiguous records while exact original charged price and tax remain
  authoritative.
- complete-schema backup/restore proof uses protected PostgreSQL administrator
  access and full migration/checksum/core-count verification;
- abandoned HelcimPay initialization cannot leave a permanent sale blocker when
  card entry never opened and no token, terminal, or provider evidence exists;
- completed return/exchange activity is visible in customer and Transaction
  Record history.

Local Playwright was intentionally skipped at operator direction. The unified
local non-E2E pre-retag gate passed before the tag moved. Exact-commit GitHub
Lint, all four blocking Playwright shards and aggregate gate, Windows and macOS
builds, and independent updater/build-manifest verification also passed.

## Included reliability hardening

The v0.95.0 release line includes checkout/cart recovery, recoverable
pending-payment controls, Helcim sale-attachment and initialization guards,
Helcim 429 backoff, station/session-scoped authenticated rate limiting,
complete-schema backup evidence, explicit customer history aggregation, the
complete tender-specific refund/exchange settlement corrections documented in
the release notes, and cache-independent long Windows server/Register release
compiles.

## Production update incident

On 2026-07-13, the published `86724e73` Windows package failed during a Main Hub update. The pre-migration dump used the limited `riverside_app` account and could not read the production-only `ros_repair_backup` schema. Rollback then restored an environment file whose application password no longer matched the PostgreSQL role, leaving the API offline until `DATABASE_URL` was synchronized from the protected deployment configuration. The restored v0.90.0 API returned HTTP 200 and the database remained intact.

The original `86724e73`, superseded `c3826bf6`, and prior `94f962c` Windows packages must not be reused. The last verified pre-candidate build `feb0db16` passed its exact-commit GitHub and release workflows. Its update package explicitly preserves installed ROSIE LLM/STT/TTS assets, environment settings, processes, and scheduled task instead of invoking ROSIE setup. A successful Main Hub update/readiness check remains required for operational signoff.

## Superseded pre-rerun publication evidence

- Before this rerun, the GitHub `v0.95.0` tag resolves to
  `f0c42ddc62295325dce6aaad3997f2f85a25f634`.
- Successful prior-build runs: Windows `30047707309`, macOS `30047707418`,
  blocking push Playwright `30047705287`, and Lint `30047705285`.
- Published prior-build Windows deployment package:
  `RiversideOS-v0.95.0-f0c42ddc-Windows-Deployment.zip`.
- Scheduled Playwright `30078123803` later failed scheduled-only coverage for
  `f0c42ddc`; it is disclosed and does not replace the successful blocking push
  run.
- These runs and assets do not certify the July 24 replacement.

## Exact-commit quality gates

| Gate | Evidence | Result |
|---|---|---|
| Unified local pre-retag gate | `npm run check:pre-retag` on `b954edad` | Passed; local Playwright excluded by operator direction |
| Lint Checks | [Run 30111733056](https://github.com/cpg716/riverside-os/actions/runs/30111733056) | Passed for the full replacement commit |
| Blocking Playwright | [Run 30111732992](https://github.com/cpg716/riverside-os/actions/runs/30111732992) | Passed; all four blocking shards and aggregate gate green |
| Windows deployment package | [Run 30111733964](https://github.com/cpg716/riverside-os/actions/runs/30111733964) | Passed; signed Register/companion assets, Main Hub server, and deployment ZIP published |
| macOS ROS Dev Center | [Run 30111733863](https://github.com/cpg716/riverside-os/actions/runs/30111733863) | Passed; universal signed macOS assets published |
| Historical local Playwright | Serialized local release suite | 382 passed, 16 expected skips, 0 failed in 21.3 minutes for an older replacement; does not certify the current candidate |
| Current replacement local Playwright | Operator direction | Intentionally skipped; exact-commit GitHub Playwright supplied the blocking evidence |

## Verified asset identity

- The final release contains 26 current, nonempty assets with GitHub SHA-256
  digests.
- Exactly one Windows deployment ZIP remains:
  `RiversideOS-v0.95.0-b954edad-Windows-Deployment.zip` (471,672,899 bytes;
  SHA-256 `6fa7e5ed4633b4f838551d1e83be50d1e2ed93940a9e0fbcd5a36af0fba61fa1`).
- All five updater manifests and all five build manifests name full commit
  `b954edadaec2a872e3529343567af01fa71da7a6` and reference present, nonempty
  signed assets.
- Three unreferenced companion-app MSI signatures from the prior build were
  removed because the corresponding MSI files were rebuilt and no current
  manifest referenced those stale signatures.
- The GitHub Release is Latest, not draft or prerelease, and its title, target,
  tag, ZIP, and manifests agree.

## Published replacement assets

- Windows deployment ZIP (installed ROSIE assets are preserved rather than
  bundled again or touched during update)
- Riverside POS updater: MSI, signature, `latest.json`, and build manifest
- Deployment Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- ROS Server Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- Counterpoint Bridge GUI: EXE/MSI installers, signatures, updater manifest, and build manifest
- macOS ROS Dev Center: universal DMG, signed app archive, updater manifest, and build manifest

## Known unresolved production incident

The retained Counterpoint false-fulfillment cohort contains exactly 567 Fulfillment Orders: 557 remain traceability reviews, one remains a current-exception review, nine have failed recognition recovery, and zero are verified. The initiating direct repair path is disabled. The proposed database guard and recovery service are retained only as non-executable design evidence under `docs/incidents/design/` and are not shipped in the migration, API, or client runtime.

The pre-retag evidence check verifies retained hashes, exact record/event sets, and current classifications, then emits a prominent non-blocking warning. Passing that check or publishing assets does not prove the initiating mutation path, complete payment/inventory/revenue/commission provenance, safe recovery, or incident resolution.

## Remaining operational signoff

The original and superseded packages failed or do not contain the July 24
replacement and must not be reused for that update. Publication does not prove
installation or production repair. Main Hub update/readiness must be checked on
the exact installed build before the deployment hold is removed. Windows
Register update/relaunch, PWA refresh/version gate, receipt/report/tag printers,
live Counterpoint connectivity, live provider credentials, production backup
evidence, read-only production audit probes, and operational signoffs remain
separate target-environment checks.
