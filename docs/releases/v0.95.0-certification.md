# Riverside OS v0.95.0 Certification Evidence

- Release tag: [`v0.95.0`](https://github.com/cpg716/riverside-os/releases/tag/v0.95.0)
- Pre-rebuild published commit verified at source freeze: `feb0db16c6574ebca35fea7f88717b273e160990`
- Source-freeze verification: 2026-07-23 UTC
- Replacement candidate at source freeze: **Pending final clean commit**
- Source-freeze status: **The prior publication passed exact-commit CI and release workflows; the 2026-07-23 Register/search/reporting/runtime replacement had not yet been tagged or published**

## 2026-07-23 replacement candidate

The replacement candidate resets every sale-local checkout drawer and Card Not Present handoff at a completed or cleared sale boundary. It accepts a hosted approval only for the exact request nonce, provider attempt, checkout, customer, and amount. **Clear Sale** releases local Register state while preserving processor evidence in Payments Health, and an informational review note cannot hide an approval that remains unlinked. Local unconfirmed recovery evidence clears only from an exact Main Hub resolution that names the checkout, recovery job, and completed Transaction Record. The new **Match Existing Paid Transaction** workflow verifies immutable transaction/payment facts and closes only the recovery record; it creates or moves no money. The same candidate also preserves non-blocking Register close with immutable Z-Report warnings, bounded and truthful search/report behavior, and the retained false-fulfillment incident evidence described below.

Local Playwright is intentionally skipped for this replacement at operator direction. Non-E2E source checks, the guarded pre-retag path, exact-commit GitHub Lint and blocking Playwright, signed packaging, updater/build-manifest verification, and physical Main Hub/terminal verification remain required. The historical publication evidence below does not certify this candidate.

## Follow-up reliability hardening

The v0.95.0 release line includes checkout/cart recovery, recoverable pending-payment controls, the Helcim sale-attachment guard, Helcim 429 backoff, station/session-scoped authenticated rate limiting, fast-fail optional Redis health checks, explicit customer timestamp aggregation, migration 132 indexes for payment/history and wedding lookup paths, the complete tender-specific refund/exchange settlement corrections documented in the release notes, and cache-independent long Windows server/Register release compiles.

## Production update incident

On 2026-07-13, the published `86724e73` Windows package failed during a Main Hub update. The pre-migration dump used the limited `riverside_app` account and could not read the production-only `ros_repair_backup` schema. Rollback then restored an environment file whose application password no longer matched the PostgreSQL role, leaving the API offline until `DATABASE_URL` was synchronized from the protected deployment configuration. The restored v0.90.0 API returned HTTP 200 and the database remained intact.

The original `86724e73`, superseded `c3826bf6`, and prior `94f962c` Windows packages must not be reused. The last verified pre-candidate build `feb0db16` passed its exact-commit GitHub and release workflows. Its update package explicitly preserves installed ROSIE LLM/STT/TTS assets, environment settings, processes, and scheduled task instead of invoking ROSIE setup. A successful Main Hub update/readiness check remains required for operational signoff.

## Pre-rebuild publication evidence

- At the 2026-07-23 source freeze, the GitHub `v0.95.0` tag resolved to `feb0db16c6574ebca35fea7f88717b273e160990`.
- Successful pre-candidate release runs: Windows `29960743064`, macOS `29960743004`, blocking push Playwright `29960741965`, and Lint `29960742016`.
- Published pre-candidate Windows deployment package: `RiversideOS-v0.95.0-feb0db16-Windows-Deployment.zip`.
- The later scheduled Playwright run `29990766855` failed nightly-only shards for `feb0db16`; it is disclosed here and does not replace the successful blocking push run.
- The release is published, but physical Main Hub update/readiness verification, printer checks, and live integration checks remain separate operational gates.

## Exact-commit quality gates

| Gate | Evidence | Result |
|---|---|---|
| Unified local pre-retag gate | `npm run check:pre-retag` on 2026-07-23 | Passed for the final source tree; local Playwright excluded by operator direction |
| Lint Checks | [Run 29960742016](https://github.com/cpg716/riverside-os/actions/runs/29960742016) | Passed for `feb0db16` |
| Blocking Playwright | [Run 29960741965](https://github.com/cpg716/riverside-os/actions/runs/29960741965) | Passed for `feb0db16`; blocking shards and aggregate gate green |
| Scheduled Playwright | [Run 29990766855](https://github.com/cpg716/riverside-os/actions/runs/29990766855) | Failed later nightly-only shards for `feb0db16`; disclosed but not the release-blocking push run |
| Historical local Playwright | Serialized local release suite | 382 passed, 16 expected skips, 0 failed in 21.3 minutes for an older replacement; does not certify the current candidate |
| Current replacement local Playwright | Operator direction | Intentionally skipped; exact-commit GitHub Playwright remains required |

## Pre-candidate signed publication evidence

| Platform | Run | Result |
|---|---|---|
| Windows full deployment | [Run 29960743064](https://github.com/cpg716/riverside-os/actions/runs/29960743064) | Passed for `feb0db16`; signed artifacts and deployment package published |
| macOS ROS Dev Center | [Run 29960743004](https://github.com/cpg716/riverside-os/actions/runs/29960743004) | Passed for `feb0db16`; universal signed assets published |
| Release publication | Exact-commit release workflows and observed GitHub asset list | Passed for `feb0db16`; the replacement still requires fresh independent verification |

Both exact-commit release runs succeeded for `feb0db16`, and the observed GitHub release contains 26 assets, including its expected short-SHA Windows deployment package and all five updater plus five build manifests. This is historical baseline evidence only; the replacement must independently prove its tag target, workflow SHA, required artifacts, manifests, signatures, and deployment-package provenance after publication.

## Published assets

- Windows deployment: `RiversideOS-v0.95.0-feb0db16-Windows-Deployment.zip` (installed ROSIE assets are preserved rather than bundled again or touched during update)
- Riverside POS updater: MSI, signature, `latest.json`, and build manifest
- Deployment Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- ROS Server Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- Counterpoint Bridge GUI: EXE/MSI installers, signatures, updater manifest, and build manifest
- macOS ROS Dev Center: universal DMG, signed app archive, updater manifest, and build manifest

The pre-candidate release is Latest, not draft or prerelease, its exact-commit GitHub workflows passed, and its published 26-asset list contains the expected updater/build manifests and short-SHA Windows deployment package. The replacement candidate requires a fresh independent manifest and release-target verification after publication.

## Known unresolved production incident

The retained Counterpoint false-fulfillment cohort contains exactly 567 Fulfillment Orders: 557 remain traceability reviews, one remains a current-exception review, nine have failed recognition recovery, and zero are verified. The initiating direct repair path is disabled. The proposed database guard and recovery service are retained only as non-executable design evidence under `docs/incidents/design/` and are not shipped in the migration, API, or client runtime.

The pre-retag evidence check verifies retained hashes, exact record/event sets, and current classifications, then emits a prominent non-blocking warning. Passing that check or publishing assets does not prove the initiating mutation path, complete payment/inventory/revenue/commission provenance, safe recovery, or incident resolution.

## Remaining operational signoff

The original and superseded packages failed or did not satisfy physical Main Hub update proof and must not be reused. Published build `feb0db16` is only the pre-candidate baseline and does not certify the July 23 source replacement. After the replacement publishes, Main Hub update/readiness must be checked on the exact installed build before the deployment hold is removed. Windows Register update/relaunch, PWA refresh/version gate, receipt/report/tag printers, live Counterpoint connectivity, and live provider credentials remain separate target-environment checks.
