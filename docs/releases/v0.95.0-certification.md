# Riverside OS v0.95.0 Certification Evidence

- Release tag: [`v0.95.0`](https://github.com/cpg716/riverside-os/releases/tag/v0.95.0)
- Release commit: `94f962c996b64f9fd0355badb69e8c775be660fe`
- Latest publication verification: 2026-07-17 UTC
- Status: **Corrected source/CI/candidates/promotion passed; Windows Main Hub operational recheck is pending**

## Follow-up reliability hardening

The pending replacement includes checkout/cart recovery, recoverable pending-payment controls, the Helcim sale-attachment guard, Helcim 429 backoff, station/session-scoped authenticated rate limiting, fast-fail optional Redis health checks, explicit customer timestamp aggregation, migration 132 indexes for payment/history and wedding lookup paths, the complete tender-specific refund/exchange settlement corrections documented in the release notes, and cache-independent long Windows server/Register release compiles. These changes are not claimed as published until the replacement tag, exact-commit workflows, and release assets are verified.

## Production update incident

On 2026-07-13, the published `86724e73` Windows package failed during a Main Hub update. The pre-migration dump used the limited `riverside_app` account and could not read the production-only `ros_repair_backup` schema. Rollback then restored an environment file whose application password no longer matched the PostgreSQL role, leaving the API offline until `DATABASE_URL` was synchronized from the protected deployment configuration. The restored v0.90.0 API returned HTTP 200 and the database remained intact.

The original `86724e73` and superseded `c3826bf6` Windows packages must not be reused. Published build `94f962c` passed the release workflow, signed Windows/macOS publication, and package/updater provenance verification. Its update package explicitly preserves installed ROSIE LLM/STT/TTS assets, environment settings, processes, and scheduled task instead of invoking ROSIE setup. A successful Main Hub update/readiness check remains required for operational signoff. This evidence will be refreshed after the pending refund/exchange replacement is published.

## Current publication evidence

- The `v0.95.0` tag resolves to `94f962c996b64f9fd0355badb69e8c775be660fe` before the pending replacement.
- Successful current release runs: Windows `29615620606`, macOS `29615620595`, blocking Playwright `29615620019`, and Lint `29615620029`.
- Published Windows deployment package: `RiversideOS-v0.95.0-94f962c9-Windows-Deployment.zip`.
- The release is published, but physical Main Hub update/readiness verification, printer checks, and live integration checks remain separate operational gates.

## Exact-commit quality gates

| Gate | Evidence | Result |
|---|---|---|
| Lint Checks | [Run 29615620029](https://github.com/cpg716/riverside-os/actions/runs/29615620029) | Passed for `94f962c` |
| Blocking Playwright | [Run 29615620019](https://github.com/cpg716/riverside-os/actions/runs/29615620019) | Passed for `94f962c` |
| Pending replacement local Playwright | Serialized local release suite | 382 passed, 16 expected skips, 0 failed in 21.3 minutes |
| Local release validation | Version parity, client lint/typecheck, locked Rust checks, deployment gate, 80 go-live gates, Help generation/impact, migrations through 132 with no drift, payment/Helcim/QBO contracts, and whitespace | Passed |

## Signed candidate evidence

| Platform | Run | Result |
|---|---|---|
| Windows full deployment | [Run 29615620606](https://github.com/cpg716/riverside-os/actions/runs/29615620606) | Passed for `94f962c`; signed artifacts and deployment package published |
| macOS ROS Dev Center | [Run 29615620595](https://github.com/cpg716/riverside-os/actions/runs/29615620595) | Passed for `94f962c`; universal signed assets published |
| Verified publication | Current release workflow | Passed; current candidate provenance and public updater assets verified |

Promotion verified that both candidate runs succeeded through the expected workflows, both runs used the tag commit, every required artifact was present and unexpired with a GitHub SHA-256 digest, updater build manifests named the exact source SHA, duplicate release names had identical bytes, and the Windows deployment ZIP contained the expected short SHA.

## Published assets

- Windows deployment: `RiversideOS-v0.95.0-94f962c9-Windows-Deployment.zip` (installed ROSIE assets are preserved rather than bundled again or touched during update)
- Riverside POS updater: MSI, signature, `latest.json`, and build manifest
- Deployment Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- ROS Server Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- Counterpoint Bridge GUI: EXE/MSI installers, signatures, updater manifest, and build manifest
- macOS ROS Dev Center: universal DMG, signed app archive, updater manifest, and build manifest

Independent post-promotion verification passed for every updater manifest and build manifest. Each published JSON manifest references `94f962c`. The release is not draft/prerelease, and the `v0.95.0` tag resolves to the exact published release commit before the pending replacement.

## Remaining operational signoff

The original and superseded packages failed or did not satisfy physical Main Hub update proof and must not be reused. Published build `94f962c` is being superseded by the refund/exchange replacement, and Main Hub update/readiness must be checked on the physical system before the deployment hold is removed. Windows Register update/relaunch, PWA refresh/version gate, receipt/report/tag printers, live Counterpoint connectivity, and live provider credentials remain separate target-environment checks.
