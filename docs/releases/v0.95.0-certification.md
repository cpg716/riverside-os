# Riverside OS v0.95.0 Certification Evidence

- Release tag: [`v0.95.0`](https://github.com/cpg716/riverside-os/releases/tag/v0.95.0)
- Release commit: `c3826bf627af2cba6a62407ce4681d1c90c0a123`
- Latest rebuild: 2026-07-13 UTC
- Status: **Corrected source/CI/candidates/promotion passed; Windows Main Hub operational recheck is pending**

## Production update incident

On 2026-07-13, the published `86724e73` Windows package failed during a Main Hub update. The pre-migration dump used the limited `riverside_app` account and could not read the production-only `ros_repair_backup` schema. Rollback then restored an environment file whose application password no longer matched the PostgreSQL role, leaving the API offline until `DATABASE_URL` was synchronized from the protected deployment configuration. The restored v0.90.0 API returned HTTP 200 and the database remained intact.

The original `86724e73` Windows package must not be reused. Corrected build `c3826bf6` passed the exact-commit Lint and aggregate Playwright lanes, non-publishing signed Windows/macOS candidates, verified promotion, and package/updater provenance verification. Its update package preserves already-installed ROSIE speech models instead of redownloading or rebundling them. A successful Main Hub update/readiness check remains required for operational signoff.

## Exact-commit quality gates

| Gate | Evidence | Result |
|---|---|---|
| Lint Checks | [Run 29284511438](https://github.com/cpg716/riverside-os/actions/runs/29284511438) | Passed for `c3826bf6` |
| Blocking Playwright | [Run 29284511484](https://github.com/cpg716/riverside-os/actions/runs/29284511484) | Four shards and aggregate gate passed for `c3826bf6` |
| Local release validation | Version parity, client lint/typecheck, locked Rust checks, deployment gate, 80 go-live gates, Help impact, migrations through 129 with no drift, focused Z-close/Staff-session/Helcim/wedding-reconciliation contracts, and whitespace | Passed |

## Signed candidate evidence

| Platform | Run | Result |
|---|---|---|
| Windows full deployment | [Run 29284612977](https://github.com/cpg716/riverside-os/actions/runs/29284612977) | Passed for `c3826bf6`; publication disabled; signed artifacts retained; no ROSIE speech-model download |
| macOS ROS Dev Center | [Run 29284612900](https://github.com/cpg716/riverside-os/actions/runs/29284612900) | Passed for `c3826bf6`; publication disabled; universal signed candidate retained |
| Verified promotion | [Run 29286861986](https://github.com/cpg716/riverside-os/actions/runs/29286861986) | Passed; candidate provenance and public updater assets verified |

Promotion verified that both candidate runs succeeded through the expected workflows, both runs used the tag commit, every required artifact was present and unexpired with a GitHub SHA-256 digest, updater build manifests named the exact source SHA, duplicate release names had identical bytes, and the Windows deployment ZIP contained the expected short SHA.

## Published assets

- Windows deployment: `RiversideOS-v0.95.0-c3826bf6-Windows-Deployment.zip` (404 MB; installed ROSIE speech models are preserved rather than bundled again)
- Riverside POS updater: MSI, signature, `latest.json`, and build manifest
- Deployment Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- ROS Server Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- Counterpoint Bridge GUI: EXE/MSI installers, signatures, updater manifest, and build manifest
- macOS ROS Dev Center: universal DMG, signed app archive, updater manifest, and build manifest

Independent post-promotion verification passed for the updater manifests and build manifests. No published JSON manifest references a superseded build. The release is not draft/prerelease, the `v0.95.0` tag resolves to the exact release commit, its assets have fresh 2026-07-13 upload timestamps, and its title records build `c3826bf`.

## Remaining operational signoff

The original package failed physical Main Hub update proof and must not be reused. Corrected build `c3826bf6` is published, but Main Hub update/readiness must be rechecked on the physical system before the deployment hold is removed. Windows Register update/relaunch, PWA refresh/version gate, receipt/report/tag printers, live Counterpoint connectivity, and live provider credentials remain separate target-environment checks.
