# Riverside OS v0.95.0 Certification Evidence

- Release tag: [`v0.95.0`](https://github.com/cpg716/riverside-os/releases/tag/v0.95.0)
- Release commit: `efe5e23406f87b73c70d49f2e5cb69b40108d40c`
- Latest publication verification: 2026-07-17 UTC
- Status: **Corrected source/CI/candidates/promotion passed; Windows Main Hub operational recheck is pending**

## Follow-up reliability hardening

The published replacement also includes checkout/cart recovery, recoverable pending-payment controls, Helcim 429 backoff, station/session-scoped authenticated rate limiting, fast-fail optional Redis health checks, explicit customer timestamp aggregation, and migration 132 indexes for payment/history and wedding lookup paths. Backdated-sale and cash-refund follow-up changes currently present in the working tree are tracked under `CHANGELOG.md` Unreleased and are not claimed as part of the published asset until they are committed and released.

## Production update incident

On 2026-07-13, the published `86724e73` Windows package failed during a Main Hub update. The pre-migration dump used the limited `riverside_app` account and could not read the production-only `ros_repair_backup` schema. Rollback then restored an environment file whose application password no longer matched the PostgreSQL role, leaving the API offline until `DATABASE_URL` was synchronized from the protected deployment configuration. The restored v0.90.0 API returned HTTP 200 and the database remained intact.

The original `86724e73` and superseded `c3826bf6` Windows packages must not be reused. Current published build `efe5e234` passed the release workflow, signed Windows/macOS publication, and package/updater provenance verification. Its update package explicitly preserves installed ROSIE LLM/STT/TTS assets, environment settings, processes, and scheduled task instead of invoking ROSIE setup. A successful Main Hub update/readiness check remains required for operational signoff.

## Current publication evidence

- The `v0.95.0` tag resolves to `efe5e23406f87b73c70d49f2e5cb69b40108d40c`.
- Successful current release runs: Windows `29543158861`, macOS `29543158860`, and blocking Playwright `29543158401`.
- Published Windows deployment package: `RiversideOS-v0.95.0-efe5e234-Windows-Deployment.zip`.
- The release is published, but physical Main Hub update/readiness verification, printer checks, and live integration checks remain separate operational gates.

## Exact-commit quality gates

| Gate | Evidence | Result |
|---|---|---|
| Lint Checks | Current v0.95.0 release workflow | Passed for `efe5e234` |
| Blocking Playwright | [Run 29543158401](https://github.com/cpg716/riverside-os/actions/runs/29543158401) | Passed for `efe5e234` |
| Local release validation | Version parity, client lint/typecheck, locked Rust checks, deployment gate, 80 go-live gates, Help impact, migrations through 129 with no drift, focused Z-close/Staff-session/Helcim/wedding-reconciliation contracts, and whitespace | Passed |

## Signed candidate evidence

| Platform | Run | Result |
|---|---|---|
| Windows full deployment | [Run 29543158861](https://github.com/cpg716/riverside-os/actions/runs/29543158861) | Passed for `efe5e234`; signed artifacts and deployment package published |
| macOS ROS Dev Center | [Run 29543158860](https://github.com/cpg716/riverside-os/actions/runs/29543158860) | Passed for `efe5e234`; universal signed assets published |
| Verified publication | Current release workflow | Passed; current candidate provenance and public updater assets verified |

Promotion verified that both candidate runs succeeded through the expected workflows, both runs used the tag commit, every required artifact was present and unexpired with a GitHub SHA-256 digest, updater build manifests named the exact source SHA, duplicate release names had identical bytes, and the Windows deployment ZIP contained the expected short SHA.

## Published assets

- Windows deployment: `RiversideOS-v0.95.0-efe5e234-Windows-Deployment.zip` (installed ROSIE assets are preserved rather than bundled again or touched during update)
- Riverside POS updater: MSI, signature, `latest.json`, and build manifest
- Deployment Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- ROS Server Manager: EXE/MSI installers, signatures, updater manifest, and build manifest
- Counterpoint Bridge GUI: EXE/MSI installers, signatures, updater manifest, and build manifest
- macOS ROS Dev Center: universal DMG, signed app archive, updater manifest, and build manifest

Independent post-promotion verification passed for every updater manifest and build manifest. Each published JSON manifest references `efe5e234`. The release is not draft/prerelease, and the `v0.95.0` tag resolves to the exact published release commit.

## Remaining operational signoff

The original and superseded packages failed or did not satisfy physical Main Hub update proof and must not be reused. Current build `efe5e234` is published, but Main Hub update/readiness must be rechecked on the physical system before the deployment hold is removed. Windows Register update/relaunch, PWA refresh/version gate, receipt/report/tag printers, live Counterpoint connectivity, and live provider credentials remain separate target-environment checks.
