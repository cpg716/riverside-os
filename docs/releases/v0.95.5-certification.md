# Riverside OS v0.95.5 Certification Evidence

- Status: **Candidate pending exact-commit GitHub Actions and asset verification**
- Version: **v0.95.5**
- Prepared: **2026-07-26**

## Source and tag

- The release source is the full commit referenced by the annotated
  `v0.95.5` tag.
- Root, client, server, Tauri, companion app, ROS Dev Center, and Windows
  deployment metadata must all report `0.95.5`.
- The final full SHA will be recorded here after the release commit and tag are
  pushed.

## Required GitHub evidence

- [ ] `main` contains the release commit.
- [ ] The remote `v0.95.5` tag resolves to that exact commit.
- [ ] Lint Checks pass for the exact release commit.
- [ ] All blocking Playwright shards and the aggregate Playwright gate pass for
      the exact release commit.
- [ ] Windows full-deployment packaging passes for the exact release commit.
- [ ] macOS ROS Dev Center packaging passes for the exact release commit.
- [ ] The GitHub release is public, non-prerelease, and marked Latest.

## Required asset evidence

- [ ] Every published asset is non-empty.
- [ ] Exactly one
      `RiversideOS-v0.95.5-<short-sha>-Windows-Deployment.zip` is present.
- [ ] Windows Register, Deployment Manager, Server Manager, CounterSync Bridge,
      and macOS ROS Dev Center updater manifests reference the exact release
      commit and their matching artifacts/signatures.
- [ ] The corresponding build manifests reference the exact release commit.
- [ ] No unreferenced or cross-version replacement asset remains.

## Local release gates

- [ ] `npm run check:pre-retag`
- [ ] `npm run check:help-impact`
- [ ] `npm run generate:help`
- [ ] `git diff --check`

## Operational boundary

Passing source and GitHub publication gates does not prove that a production
Main Hub installed the build. Production signoff still requires the installed
version and SHA, `/api/ready`, database-backed lookup, workers/dependencies, and
`/api/health`, followed by Register, printer, payment-terminal, backup, and
store-workflow checks on the physical environment.
