# Riverside OS v0.95.5 Certification Evidence

- Status: **Published and independently verified**
- Version: **v0.95.5**
- Prepared: **2026-07-26**

## Source and tag

- The release source is
  `e3307d38ba3b4873d0c9ad8a4dc972e08c438940`, referenced by the annotated
  `v0.95.5` tag.
- Root, client, server, Tauri, companion app, ROS Dev Center, and Windows
  deployment metadata must all report `0.95.5`.
- Remote `main`, the remote tag, the GitHub release target, and all updater
  build manifests resolve to that exact SHA.

## Required GitHub evidence

- [x] `main` contains the release commit.
- [x] The remote `v0.95.5` tag resolves to that exact commit.
- [x] Lint Checks passed in run `30214157888`.
- [x] All four blocking Playwright shards and the aggregate gate passed in run
      `30214157880`.
- [x] Windows full-deployment packaging passed in run `30214158646`.
- [x] macOS ROS Dev Center packaging passed in run `30214158650`.
- [x] The GitHub release is public, non-prerelease, and marked Latest.

## Required asset evidence

- [x] All 26 published assets are non-empty.
- [x] Exactly one
      `RiversideOS-v0.95.5-e3307d38-Windows-Deployment.zip` is present.
- [x] Windows Register, Deployment Manager, Server Manager, CounterSync Bridge,
      and macOS ROS Dev Center updater manifests reference the exact release
      commit and their matching artifacts/signatures.
- [x] The corresponding five build manifests reference the exact release
      commit.
- [x] No unreferenced or cross-version replacement asset remains.

## Local release gates

- [x] `npm run check:pre-retag`
- [x] `npm run check:help-impact`
- [x] `npm run generate:help`
- [x] `git diff --check`

## Operational boundary

Passing source and GitHub publication gates does not prove that a production
Main Hub installed the build. Production signoff still requires the installed
version and SHA, `/api/ready`, database-backed lookup, workers/dependencies, and
`/api/health`, followed by Register, printer, payment-terminal, backup, and
store-workflow checks on the physical environment.
