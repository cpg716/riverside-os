# Riverside OS v0.95.5 Certification Evidence

- Status: **Exact-build certification is determined from the live tag,
  workflows, assets, and manifests described below**
- Version: **v0.95.5**
- Prepared: **2026-07-26**

## Source and tag

- The release source is the exact commit referenced by the remote annotated
  `v0.95.5` tag. The prior `e3307d38` evidence is superseded and cannot certify
  replacement assets.
- Root, client, server, Tauri, companion app, ROS Dev Center, and Windows
  deployment metadata must all report `0.95.5`.
- Remote `main`, the remote tag, the GitHub release target, and all updater
  build manifests resolve to that exact SHA.

## Required GitHub evidence

- `main` contains the release commit.
- The remote `v0.95.5` tag resolves to that exact commit.
- Exact-commit Lint Checks passed.
- All four blocking Playwright shards and the aggregate gate passed.
- Windows full-deployment packaging passed.
- macOS ROS Dev Center packaging passed.
- The GitHub release is public, non-prerelease, and marked Latest.

## Required asset evidence

- All published assets are non-empty.
- Exactly one current-build
  `RiversideOS-v0.95.5-<sha>-Windows-Deployment.zip` is present.
- Windows Register, Deployment Manager, Server Manager, CounterSync Bridge, and
  macOS ROS Dev Center updater manifests reference the exact release commit and
  their matching artifacts/signatures.
- The corresponding five build manifests reference the exact release commit.
- No unreferenced or cross-version replacement asset remains.

## Local release gates

- [x] `npm run check:pre-retag`
- [x] `npm run check:help-impact`
- [x] `npm run generate:help`
- [x] `git diff --check`

## Operational boundary

Passing source and GitHub publication gates does not prove that a production
Main Hub installed the build. Production signoff still requires the installed
version and SHA, `/api/ready`, database-backed lookup, workers/dependencies, and
`/api/health`, followed by Register, printer, payment-terminal, backup, and
store-workflow checks on the physical environment.
