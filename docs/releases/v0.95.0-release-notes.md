# Riverside OS v0.95.0 Release Notes

- Status: **Replacement published — Windows Main Hub operational verification pending**
- Target version: **v0.95.0**
- Prepared: **2026-07-14**

## Latest rebuild

> **Windows deployment hold (2026-07-13):** Do not use the original `86724e73` or superseded `c3826bf6` Windows deployment packages for another Main Hub update. Production-machine verification exposed an administrator-vs-app backup permission error followed by incomplete rollback credential recovery, and review of the streamlined package found that its update entry points could still invoke ROSIE setup. The Main Hub was restored on v0.90.0 without database loss. Corrected build `6d91610a` adds explicit update-only ROSIE preservation and passed exact-SHA CI, signed-candidate, promotion, and updater-asset verification. Physical Main Hub readiness verification is still required before broad deployment.

> **Replacement rebuild (2026-07-14):** Published build `6a945e57` adds customer-merge, wedding appointment, wedding financial-view, Register customer-search, and migration/test-database integrity hardening. Exact-SHA Lint and aggregate Playwright passed, both signed candidates passed, promotion verified their provenance, and every updater/build manifest identifies `6a945e57`. The physical Main Hub verification hold remains in effect until this Windows package completes an installed-machine update/readiness check.

- **Single-day Z-Report recovery** — Register close now resolves and closes the oldest unclosed store-local business date, never combines multiple days, and preserves each day's tenders, transactions, adjustments, inventory activity, QBO staging, deposit evidence, and Daily Financial Report separately. Migration 126 repairs the July 10 and July 11 production reports as distinct business days without inventing a historical drawer count.
- **Main Hub replacement-package safety** — all update paths now require a verified PostgreSQL administrator backup before stopping the existing server or replacing files, reject incomplete dumps, encode database credentials safely, and attempt prior-task restoration/restart independently when later rollback work fails.
- **Faster and safer Windows update packaging** — Windows deployment packages no longer re-download and re-bundle ROSIE speech models already held on production Main Hubs. Every Main Hub update entry point now uses an explicit preservation mode that leaves installed LLM/STT/TTS assets, ROSIE environment settings, active processes, and the LLM scheduled task untouched. If an earlier failed update removed the model path from `.env`, the updater safely rediscovers the already-installed GGUF without downloading or replacing it. Fresh installations and intentional ROSIE repairs retain the pinned full-setup path.
- **Line-level tax controls** — Register sale lines preserve explicit Standard, Clothing, and No Tax selections through pricing, discounts, checkout, server validation, audit metadata, and persisted sale detail; shipping and alteration labor remain server-enforced non-taxable charges.
- **Canonical fee and gift-card controls** — legacy Shipping/Alterations pseudo-products are archived without deleting history; typing SHIPPING or ALTERATIONS opens the correct non-taxable fee path; gift-card tenders show and revalidate the card's pre-sale balance and preserve Regular, Loyalty, Donated, or Promo accounting evidence; Product Hub keeps logical variation order and no longer jumps to the top after a tag print.
- **Legacy Counterpoint order repair** — Settings can now scan the historical Counterpoint data already stored in ROS, reconcile only exact order/ticket/payment matches into the original customer order, retain an audit snapshot, and leave ambiguous accounts unchanged for Manager review without running another import.
- **Approved Helcim sale recovery** — Payments Health can match one exact retained cart to an approved but unlinked Helcim charge and let an authorized manager create the missing Transaction Record through normal checkout logic without charging the card again, while preserving the original approval time, provider link, operator, manager, and audit trail.
- **Approved Helcim order-payment recovery** — Payments Health can safely allocate an approved unlinked terminal payment to an exact open customer Transaction Record, recalculate its balance, and preserve the provider match and recovery audit; Register also locks approved provider tenders against removal, parking, clearing, or accidental replacement.
- **Wedding split-deposit reconciliation** — group-pay tenders now retain the payer on every payment record, payer Customer History shows the party and funded-member count, Wedding Manager distinguishes held deposits from unpaid members, and Daily Sales/print/CSV output separates the payer's own Transaction Record from wedding deposits placed while reconciling the complete tender exactly once. Migration 129 deterministically backfills missing checkout payer links from their persisted source Transaction Records.
- **Customer and wedding data protection** — Register customer lookup now cancels stale responses; duplicate-customer merges fail closed when related measurements, alterations, balances, relationships, or operational history cannot be safely reassigned; wedding schedule-conflict overrides commit the appointment and Manager audit together; and parties holding deposits without merchandise Transaction Records return zero-value economics instead of failing their financial/readiness view.
- **Migration and integration-test integrity** — the active baseline now includes migration 130, and the dedicated integration database must be refreshed and verified through migration 130 before the next release build. Counterpoint reconciliation tests no longer inherit an application database connection, and migration checksum verification accepts only known line-formatting variants while continuing to block executable SQL drift.
- **Production hardening across financial and data writes** — wedding group-pay allocations now lock and re-check beneficiary balances; shipping, refund, exchange, financial-date, status, and order-line mutations keep required audit history inside the same database transaction; strict production startup fails closed when employee pricing cannot be loaded.
- **Staff Access session integrity** — replaced retained raw-PIN authentication with hashed opaque sessions bound to the workstation and individual app/tab connection, including expiry, independent revocation, PIN/deactivation invalidation, active-session Station Fleet evidence, and a dedicated sign-in rate limit.
- **Staff Access expiry reliability** — long expiry intervals are scheduled in browser-safe chunks and rechecked, so clock skew cannot overflow the timer and immediately revoke a valid new session.
- **Connection performance and desktop/PWA safety** — warmed and bounded PostgreSQL pooling, added shared outbound HTTP keepalive/timeouts, throttled Staff/Register activity writes, protected the Tauri WebView with an explicit CSP, and kept PWA/LAN/Tailscale reconnect monitoring adaptive and non-blocking.
- **Register audit continuity** — Z-close reconciliation now uses scoped Staff/Register sessions instead of a retained PIN, close/handoff actions resolve the real authenticated staff actor, and post-sale commission-attribution corrections request a fresh Access PIN.
- **Backup, migration, and failure visibility** — migration 125 adds revocable Staff Access sessions; the backup/restore drill recognizes only approved test sandboxes and verifies the latest migration; Counterpoint export, staff-cap, and Register-search failures are now visible instead of appearing successful or empty.
- **Documentation and permanent gates** — refreshed Staff, POS, Remote Access, deployment, security, and production-hardening guidance; added focused session/reconciliation contracts; and expanded the permanent go-live gate to 80 checks.
- **ROSIE universal search reliability** — stabilized the receipt-order navigation callback so search state updates do not restart the same debounced query or submit duplicate ROSIE search-intent requests.
- **Focused local validation** — fresh and dirty-database migrations through 129 passed with no checksum drift before this follow-up; migration 130 is covered by layout/parser validation and awaits the next fresh/dirty database rehearsal. The unified pre-retag gate passed version parity, deployment/release guards, all 80 go-live blockers, Rust formatting and migration parser tests, client lint/typecheck, Help generation/impact, and whitespace checks. Exact-SHA GitHub Lint and aggregate Playwright also passed before the replacement candidates were built.

## Why v0.95.0

Riverside OS changed substantially during the v0.90.0 development and repeated hardening cycle. Moving directly to v0.95.0 makes that distance visible while preserving v1.0.0 for the formal production-maturity milestone. This is a new versioned release, not another same-version rebuild of v0.90.0.

## Major advances represented by v0.95.0

- **Financial and checkout integrity** — exact-cent tax and total parity, stronger tender and recovery boundaries, auditable deposit/payment allocation, QBO mapping and liability corrections, and safer post-checkout recovery.
- **Wedding party payments** — split wedding deposits are visible on the beneficiary customer, notify Register staff, can be applied during final payment, and retain correct liability, allocation, void, and fulfillment accounting.
- **Help Center and ROSIE** — expanded staff manuals and screenshots, searchable ROSIE Help coverage, and an on-demand printable RiversideOS User Manual PDF generated from the current effective manuals.
- **Register and printing reliability** — hardened native Tauri routing for receipts, Reports, and Riverside LP 2844 EPL tags, with bounded dispatch, retry visibility, and truthful success messaging.
- **Counterpoint transition tooling** — direct Bridge-to-ROS import proof, run-scoped provenance and exceptions, stronger catalog/open-document recovery, and clearer Main Hub operational status.
- **Provider and operational resilience** — hardened Helcim, QBO, IMAP, Shippo, connectivity, backup/update, offline recovery, and transactional outbox boundaries without placing remote provider waits inside customer-money locks.
- **Windows deployment and updates** — rollback-safe Main Hub installation, stronger migration/bootstrap recovery, signed standalone updaters, exact-build deployment selection, and clearer Deployment/Server Manager diagnostics.
- **Release engineering** — Node 24 and Rust 1.91 build tooling, immutable action pins, bounded Dependabot load, per-job Rust caches, concurrent Windows/macOS builds, aggregate Playwright enforcement, and exact-SHA release-candidate promotion.

## Release method

v0.95.0 must use the verified candidate path:

1. Commit the complete v0.95.0 source and documentation with all version manifests in parity.
2. Pass Lint Checks and blocking Playwright on that exact commit.
3. Run the Windows and macOS release workflows with `publish_release=false`.
4. Confirm both candidate runs completed successfully and retained all required signed artifacts.
5. Create `v0.95.0` on that exact candidate commit.
6. Run **Promote verified release candidates** with both run IDs.
7. Verify published updater manifests, build manifests, signatures, deployment ZIP, release title/body, and installed-machine update behavior.

The promotion workflow fails closed when the tag SHA, Windows run SHA, macOS run SHA, GitHub artifact digests, updater build manifests, or deployment package provenance do not agree.

## Validation status

- Version parity: passed locally for v0.95.0.
- Local release gates: deployment/release guards, 80 go-live blockers, client lint/typecheck, Rust build/check/tests, Help generation/impact, clean migration rehearsal, and focused browser/API contracts passed.
- Exact-SHA GitHub Lint [run 29292515155](https://github.com/cpg716/riverside-os/actions/runs/29292515155) passed for `6d91610a`.
- Blocking Playwright [run 29292515142](https://github.com/cpg716/riverside-os/actions/runs/29292515142) passed all four shards and its aggregate gate for `6d91610a`.
- Signed Windows [run 29293110479](https://github.com/cpg716/riverside-os/actions/runs/29293110479), signed macOS [run 29293110445](https://github.com/cpg716/riverside-os/actions/runs/29293110445), and verified promotion [run 29294454453](https://github.com/cpg716/riverside-os/actions/runs/29294454453) passed for `6d91610a`.
- Public-release verification passed for all updater manifests, build manifests, signatures, and the 404 MB `RiversideOS-v0.95.0-6d91610a-Windows-Deployment.zip`; every published JSON manifest references `6d91610a`.
- Exact-SHA GitHub Lint [run 29330650404](https://github.com/cpg716/riverside-os/actions/runs/29330650404) and blocking Playwright [run 29330650363](https://github.com/cpg716/riverside-os/actions/runs/29330650363) passed for `6a945e57`; Playwright passed all four shards and its aggregate gate.
- Signed Windows full-deployment [run 29330711534](https://github.com/cpg716/riverside-os/actions/runs/29330711534), signed macOS [run 29330712815](https://github.com/cpg716/riverside-os/actions/runs/29330712815), and verified promotion [run 29334676456](https://github.com/cpg716/riverside-os/actions/runs/29334676456) passed for `6a945e57`.
- Public-release verification passed for every updater manifest, build manifest, installer signature, companion application, macOS package, and the 404 MB `RiversideOS-v0.95.0-6a945e57-Windows-Deployment.zip`; every published JSON manifest references `6a945e57`.
- Production-machine update verification: failed for the original `86724e73` Windows package on 2026-07-13; physical verification of current build `6a945e57` is pending.

v0.95.0 remains the current release line. Current build `6a945e57` is published, but broad Windows Main Hub deployment remains on hold until that package completes a physical Main Hub update/readiness check. Printer and live integration validation remain separate operational signoff items rather than source/build claims.
