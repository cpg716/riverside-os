# Riverside OS v0.60.0 Production Certification

**Date:** 2026-05-17  
**Target release:** `v0.60.0`  
**Release commit:** Final release commit is the annotated `v0.60.0` tag target.  
**Tag:** `v0.60.0` pending publication  

## 1. Production Release Audit

The release pass is scoped to production readiness, version/build alignment, Help/ROSIE currency, deployment reproducibility, recovery readiness, issue packaging, and final release execution. No speculative module rewrites or unrelated cleanup are included.

## 2. Critical Blockers Found

- Local `v0.60.0` software gates passed on 2026-05-17.
- Pending GitHub release tag and release assets.
- Pending human/environment signoffs for production hardware, backup/restore, payments, QBO/accounting, and store operations.

## 3. Version/Build Alignment Status

All release-bearing metadata is aligned to `0.60.0` across root package, client/PWA package, server crate, Tauri crate, and Tauri config. `npm run check:version` passed on 2026-05-17.

## 4. Deployment/Update Readiness

Windows updater and deployment package workflows are the release artifact source of truth. Final release requires `latest.json`, `riverside-updater-build-manifest.json`, signed Windows MSI, matching `.sig`, and a `RiversideOS-v0.60.0-<sha>-Windows-Deployment.zip` package whose manifest source SHA matches the release tag.

## 5. Error + Issue System Status

The existing staff issue pipeline supports manual bug reports with screenshots, written reproduction context, role/runtime/navigation/build metadata, client diagnostics, correlation IDs, and bounded server log snapshots. Automated error events capture error toasts and unhandled browser/WebView failures.

## 6. AI Issue Packaging Status

Settings -> Bug reports exposes the full report JSON as an AI diagnostic package. The package is suitable for Codex/engineering handoff because it keeps reproduction notes, runtime context, logs, screenshots, and relevant IDs together.

## 7. Help Center + ROSIE Status

Help/ROSIE corpus validation passed on 2026-05-17. The Help manifest/corpus was regenerated with screenshots skipped, and `scripts/verify-ai-knowledge-drift.sh` reported 109 manifest paths present with no unlisted staff guides.

## 8. Certification Status

| Gate | Status |
| --- | --- |
| `npm run check:version` | Passed 2026-05-17 (`Riverside version parity OK: 0.60.0`) |
| `git diff --check` | Passed 2026-05-17 |
| `cargo fmt --manifest-path server/Cargo.toml` | Passed 2026-05-17 |
| `npm --prefix client run lint` | Passed 2026-05-17 |
| `npm --prefix client run typecheck` | Passed 2026-05-17 |
| `npm run check:server` | Passed 2026-05-17 |
| `npm --prefix client run build` | Passed 2026-05-17 |
| `npm run test:e2e:release` | Passed 2026-05-17 (`337 passed`, `12 skipped`) |
| `npm run generate:help:refresh -- --skip-screenshots` | Passed 2026-05-17 |
| `scripts/verify-ai-knowledge-drift.sh` | Passed 2026-05-17 |
| GitHub Lint Checks on final commit | Pending |
| GitHub Playwright E2E on final commit | Pending |

## 9. Release Artifact Status

Pending final commit, tag, GitHub release, updater workflow, deployment package workflow, asset download, and manifest/source-SHA verification.

## 10. Operational Governance Status

Owner, accounting, store operations, hardware, and backup/restore signoffs remain required before unattended production use unless explicitly waived in writing.

## 11. Rollback Readiness

Rollback requires the pre-update database backup and the matching previous server binary, web bundle, and Windows installer. Running an older binary against a newer migrated database without restore is unsupported.

## 12. Remaining Production Risks

- Production DB probes must be run read-only against the target release database.
- Hardware/register peripherals require physical station smoke.
- Payment provider and QBO behavior require live/provider-context review.
- Backup/restore must be rehearsed on non-production data before live rollout.

## 13. Final Release Checklist

- [x] Version parity passes at `0.60.0`.
- [x] Local lint/typecheck/server/build gates pass.
- [x] Release E2E gate passes.
- [x] Help/ROSIE corpus is current.
- [ ] Final release commit is pushed to `main`.
- [ ] Annotated tag `v0.60.0` points to the final release commit locally and remotely.
- [ ] GitHub release is published from `v0.60.0-github-release.md`.
- [ ] Windows updater and deployment package artifacts are verified.
- [ ] Human/environment signoffs are complete or explicitly waived.

## 14. Final Go/No-Go Verdict

**NO-GO for store rollout until release artifacts and required production signoffs are complete.**

Software certification is **GO** for release commit/tag publication based on local gate evidence above.
