# Riverside OS v0.96.0 Release Notes

- Status: **Same-version updater rebuild. The final committed source must pass
  the complete pre-retag gate and exact-commit Playwright coverage;
  publication requires exact-commit GitHub Playwright, successful Windows and
  macOS packaging, and verified updater manifests.**
- Target version: **v0.96.0**
- Prepared: **2026-08-02**
- Rebuilt: **2026-08-13**
- Baseline: **v0.95.5** (`56de49dc547803a43e31c1acd9bce7bb6847d8bc`)

## Release summary

v0.96.0 turns the recent Wedding Collect & Build work into a clearer,
restart-safe production workflow. Staff can prepare exact items for every party
member, review funding and attribution, then create the separate member-owned
Transactions and Wedding Fulfillment Orders in one guided action through the
existing audited checkout path. The release also makes product variation
selection easier to navigate and aligns Main Hub audit/repair scripts with the
real production installation contract. ROSIE now ships as a fully certified
local Gemma 4 stack, and staff workflows consistently support keyboard, mouse,
touch, and assistive input.

The original release contained these six product commits after v0.95.5:

- `282e41aa` — variation-picker navigation and regression coverage.
- `88a82ae8` — completed staff and domain guidance for the picker workflow.
- `ef062c44` — one-action wedding order building, Deposit Only resume, stable
  member checkout identities, receipts, and focused E2E contracts.
- `1e8b4a91` — production-aligned Main Hub auditing and credential-key repair.
- `803f1133` — complete local Gemma 4 ROSIE integration, multimodal Help,
  native read-only tools, streaming, health certification, and installer
  rollback safeguards.
- `3321ffa2` — input-method accessibility across staff workspaces, overlays,
  search results, wedding controls, and shared interaction styles.

This same-version rebuild also includes:

- a simplified Operations home organized around **Do Now**, **Needs Follow-Up**,
  and collapsed **Healthy systems** evidence. Live Main Hub readiness, verified
  backups, workstation heartbeats, and open Register sessions replace manual
  daily guesses, while certification, alert history, performance, integration,
  and Bug Manager evidence remain available under Advanced Diagnostics;
- a reviewed ROS-to-QBO GL crosswalk. QuickBooks Mapping pairs the complete
  approved Riverside GL catalog with Intuit's live QBO accounts, suggests exact
  and high-confidence Riverside numbers without auto-approving them, shows
  match/review state, and preserves the ROS reference through inline staging
  corrections. The server continues to validate the active QBO account as the
  authoritative posting destination;
- a staff-visible Review Request outbox for scheduled invitations, including
  cancellation before delivery and an authorized ROS-native test-send action.
  Test requests use the normal Riverside Customer, Podium review-link,
  consent, tracked delivery, and audit paths; staff do not open Podium directly
  or invent an external send;
- fail-closed exchange refund completion. An exchange that owes the customer
  cannot show Sale Complete until the exact amount is issued to Cash, the
  linked Original Card, Gift Card, or Store Credit. Pending Transaction Records
  and receipts remain visibly **Refund pending** instead of printing a false
  settlement. Original-card exchange remainders receive a read-only Helcim
  batch and capacity preflight before the replacement Transaction is recorded,
  then repeat the authoritative capacity and batch validation at settlement.
  Full reversal eligibility uses the complete provider charge rather than a
  wedding/member allocation, preventing an oversized reversal of shared or
  deposit-funded card payments;
- a safer, faster Operations Mailbox with conversation-level read state,
  multi-select triage, recoverable Trash, and a scriptless contained HTML
  viewer. Podium conversation history now merges webhook and provider-pull
  messages into one regular Inbox flow, keeps incomplete pulls visibly
  incomplete, and leaves unknown senders in that same flow with Match or Add
  Customer actions instead of splitting them into a separate review queue;
- a conflict-safe, auditable appointment system with explicit duration,
  resource/capacity controls, revision guards, lifecycle history, reasoned soft
  cancellation, exact Search/Operations/Customer deep links, and bounded
  confirmation/reminder retries. The appointment editor is viewport-bounded and
  scrollable on phone-sized screens so its Close and save actions remain
  reachable, with focused scheduler and Operations Center regressions covering
  phone, tablet, iPad, and desktop behavior;
- a streamlined, staff-friendly Podium Inbox with a compact text-message layout,
  exact-conversation notification routing, sender context in notification
  previews, and visible conversation ownership. Managers link each Riverside
  staff profile to the corresponding Podium user from the provider directory;
  the Inbox then maps live Podium assignees to Riverside staff and allows an
  assignment or unassignment without sending a reply. Each conversation also
  remembers a **Replying as** staff member from that same active, Podium-linked
  roster after the selected person's one-time Access PIN verification; the
  server refuses an unlinked reply identity. Opening a conversation marks the
  shared Riverside Inbox state read, staff can mark individual or selected
  conversations Read or Unread,
  and confirmed Close / Reopen actions update Podium's native closed state with
  partial-batch failure reporting and a reversible Closed filter. An unmatched
  sender can receive a reply before staff link or create a Customer; its phone
  number is the primary label until staff link it, with **Unknown sender**
  reserved for events that contain no usable identifier. The composer provides
  optional check-in and pickup drafts, common emoji, and a validated PNG
  attachment for SMS. Selected conversations, primary actions, and Riverside
  outbound messages use a consistent blue Inbox identity. Customer Relationship
  Hub presents one clear Text or Email history instead of duplicating that
  activity in a second communication timeline;
- signed Podium call and review lifecycle activity in Riverside. Missed calls,
  completed calls, and voicemail indicators appear chronologically in the linked
  Inbox conversation. Nested call payloads retain their phone, contact,
  conversation, and provider timestamps; the conversation list identifies a
  latest call as **Call**, while Inbox Status shows the stored-call count and
  latest arrival so a missing provider call subscription is actionable.
  Published reviews and business responses appear in Operations → Reviews with
  Customer/Transaction attribution, provider links, and **Needs response**
  filtering, and attributable reviews also appear in the related Inbox thread;
- Z-Report history that prefers the immutable total stored in each saved close
  snapshot instead of recalculating historical totals from today's ledger.
  Only legacy snapshots without a saved total use compatibility reconstruction;
  history and ROSIE lists omit large saved-detail arrays through the compact
  response contract, while opening a report still loads its complete booked-day
  evidence;
- a more visual and resilient native Insights workspace. Up to two governed
  reports can generate in the background while staff move through Riverside,
  with visible progress, completion notifications, and a separate New Report
  path that does not overwrite an in-progress refinement. Staff can switch
  among table, bar, line, area, and pie presentations without rerunning the
  governed query, and Print / PDF includes the visible chart by default with a
  reviewed data-only option;
- completion of the guarded Counterpoint Gift Card repair. The preview now
  fingerprints authoritative original-value corrections as well as payment
  classifications, apply refuses source drift, current balances remain
  unchanged, and every corrected original amount receives staff-attributed
  audit evidence. Back Office code lookup can also open depleted, voided, and
  expired cards for retained customer and activity history without weakening
  the active-card checks used by Register checkout;
- controlled Counterpoint repair reruns. A one-shot bridge run can restrict
  writes to an explicit validated entity list while retaining the complete
  source preflight, protected-dependency checks, and audited `fix_rerun`.
  An immediately retried interrupted run can reuse the exact passed preflight
  UUID, which ROS revalidates before opening the new audited import run, and
  Main Hub request failures preserve their underlying network cause. Payment-
  only duplicate recovery also fails closed for zero or multiple candidate
  Transactions instead of indexing an empty match list. Counterpoint's own
  reviewed paid-price repair and reconciliation events no longer masquerade as
  staff activity during source replacement; real return, refund, exchange,
  allocation, fulfillment, procurement, and line-audit dependencies remain
  protected;
- corrected historical booked-sales classification for Counterpoint tickets
  that contain only lifecycle `U` rows. On a source rerun, ROS excludes the
  legacy initial booking event from sales reporting without deleting or
  rewriting the retained Transaction, lines, payments, returns, fulfillment
  state, or audit history. Prior-year comparisons, forecasting, and inventory
  movement reporting therefore use the same financial-sale basis as current
  ROS booked sales while preserving the complete operational record;
- Podium developer-portal webhook tests now accept the provider's signed test
  sentinel as a verified no-op while continuing to reject arbitrary eventless
  payloads;
- repair of the Windows Cube migration path that failed before server
  replacement. The installer now defines and invokes
  `Ensure-OptionalReportingRole`, provisions the required `cube_ro` login,
  and routes migration 185 through its transaction-scoped compatibility helper.
  Windows packages include the pinned non-Docker Cube runtime and portable Node
  host; installation supervises it as `Riverside OS Cube Core`, verifies
  loopback readiness, and rolls its files, task, and role password back with the
  rest of a failed update. Packaging retries a transient Cube dependency
  download failure up to three times with bounded backoff, while a final failed
  attempt still blocks publication;
- native conversational **Insights** backed by the Riverside reporting model,
  replacing the embedded Metabase workflow with permission-aware natural-language
  report building, refinement, favorites, history, CSV export, and Reports-printer
  output while preserving booked-versus-fulfilled and financial-source boundaries;
- a governed ROSIE + Cube Core Insights engine. Cube remains a loopback-only
  semantic service and reads the shared Riverside PostgreSQL database through
  the restricted `cube_ro` role and approved `reporting.*` views; it does not
  own a separate reporting database or provide a separate staff login. Riverside
  validates report specifications and staff permissions before signing requests
  to Cube, including Admin-only cost and margin controls;
- corrected Insights planner capacity and readiness. Planner requests no longer
  duplicate the complete semantic catalog in both the prompt and constrained
  tool schema, and the Main Hub host now provides an effective 8,192-token
  context to each of its two concurrent ROSIE slots. The workspace reports
  **Reporting ready** only when both Cube Core and the selected ROSIE planner
  are configured and reachable;
- Insights now remains inside the standard Back Office workspace instead of
  switching to a separate shell, so staff retain the normal sidebar, top bar,
  authenticated identity, and navigation while building and reviewing reports;
- independent Podium SMS enablement for staff-authored texts, text receipts, Ready
  for Pickup, alteration-ready, appointment confirmation, appointment reminder,
  and unknown-sender welcome messages. Each switch is enforced by the server at
  its own send path, every message remains editable, and an existing legacy master
  SMS setting migrates to the matching individual switches during update;
- rollback-safe Main Hub execution of the governed Insights migration: the
  installer temporarily gives the Riverside application role only the
  role-management authority migration 185 needs, executes the migration in the
  existing database transaction, restores the prior role attributes before
  commit, and rolls back both schema and privilege changes if any step fails;
- corrected ROSIE speech and installed-knowledge settings for the pinned Kokoro
  multilingual model. The voice picker now exposes the model's real US-English
  Maple and Sol voices plus British-English Vale, old saved speaker numbers
  safely migrate to Maple, and server, desktop, and certification paths cannot
  select the model's Chinese speaker IDs. SenseVoice transcription is explicitly
  fixed to English with normalized text in both runtime paths and certification.
  Main Hub updates verify Gemma in the foreground, then run the Kokoro-to-SenseVoice
  loopback under the hidden watchdog after server readiness instead of printing
  native speech diagnostics through the updater. Approved Help, staff, and ROSIE
  policy sources are embedded into installed releases, while build-only refresh
  actions and optional Meilisearch state are represented accurately in Settings;
- repaired Counterpoint gift-card classification from `SY_GFC`, with explicit
  Sold / Purchased, Loyalty, Donated, and Promo types instead of silent purchased
  fallback. Loyalty, Donated, and Promo cards use one calendar year from issue,
  Back Office labels expired cards clearly, and the guarded imported-card repair
  verifies the complete scoped source count and balance before changing metadata
  without changing card balances or statuses. Authoritative source issue amounts
  and retained history are repaired only through the separately reviewed,
  fingerprint-bound apply path.
  Attended Main Hub repair uses staff authorization and the direct Counterpoint
  source rows without depending on the bridge process or machine token.
  Card Inventory now also pages through the complete filtered PostgreSQL result,
  shows its exact range and total, and no longer stops silently at 100 rows;
- hardened ROS-issued Loyalty and Donated cards so their issue events retain the
  staff actor, award letters and History use the saved one-year expiration, and
  manual/donated issuance requires an approval reason. Donated redemption now
  uses its own Settings-managed QBO expense mapping rather than Loyalty/Promo
  expense or purchased-card liability;
- restored complete Gift Card audit visibility. Imported Counterpoint cards can
  receive an idempotent source issue event without changing balances, activity
  retrieval failures are shown instead of appearing as an empty history, and
  new Register redemption events retain the acting staff member. Gift Card and
  QBO detail overlays use the shared portal and remain centered in the visible
  workspace after the underlying page has scrolled;
- added an authenticated, preview-bound repair for historical imported Gift Card
  payment classifications. Apply must match the reviewed preview token, updates
  only scoped metadata, records the staff actor and before/after audit evidence,
  and returns the affected activity dates that must have their QBO proposals
  regenerated before accounting review;
- completed flexible loyalty reward-card fulfillment for individual and
  selected-group issuance. Staff can load one or more configured $50 reward
  blocks onto each scanned Loyalty Gift Card, split the available reward across
  as many or as few cards as needed, or intentionally leave blocks for later.
  POS and Back Office preview each load and lock card entry while issuance is in
  progress, while grouped letters, deduplicated mailing labels, mobile-safe
  eligibility and History actions, authenticated point adjustments, and
  server-backed request idempotency prevent duplicate deductions or card loads;
- corrected existing-order amendment reporting so added or removed merchandise
  is booked as a signed net change on the store-local amendment date without
  moving the original Transaction date. Same-day reductions lower Booked
  Subtotal without inflating the sale count, and removal of the final item
  remains visible in Daily Activity and audit history;
- manual dispatch recovery for the existing Lint and Playwright workflows so
  an exact-commit release can be certified when GitHub push webhooks are
  throttled, without changing either workflow's validation jobs;
- Helcim terminal **Try Again** recovery that keeps the original checkout and
  invoice pending after an individual card decline, clearly guides staff to
  retry on the reader, and attaches one later unique approval without a
  duplicate Card Reader request or Manual Card entry;
- recoverable unresolved Card Not Present handoffs. If provider recovery cannot
  prove approval, decline, or cancellation, staff can explicitly release the
  unresolved hosted attempt from the sale after a clear warning. Riverside does
  not label the attempt declined or erase it: Payments Health retains the
  provider token and audit evidence, a late signed confirmation can still
  promote it, and a later exact approval releases older unresolved locks for the
  same checkout without creating another payment;
- Podium webhook and Inbox recovery that accepts both documented camelCase and
  snake_case signed metadata, uses the provider event UID for idempotency, and
  correctly classifies inbound and failed messages for notification processing.
  Message-history fallback uses Podium's cursor-only conversation-history
  contract, runs when history is more than 30 minutes old, and marks a matched
  conversation current only after its complete history is stored. Partial pulls
  remain visibly incomplete across refreshes, stale webhook failures clear only
  after a newer accepted delivery, and the Inbox distinguishes local Riverside
  webhook readiness from the provider subscription state owned by Settings;
- Daily Financial Report delivery that always generates and archives enabled
  reports, validates recipient addresses, records only successful deliveries,
  and safely retries failed or partially delivered reports without duplicate
  sends to recipients who already received them;
- Daily Financial Report headline and comparison figures that use the same
  canonical booked net-sales calculation as ROS Today's Sales and the booked
  Register report. The email now adds current month-to-date booked net, the
  same calendar-day month-to-date window last year, signed dollar and percentage
  changes, and truthful business-day Visual Crossing weather. Fulfillment-based
  recognized revenue remains separately labeled in the body, mock weather is
  never presented as actual financial-report data, and report history preserves
  an explicit legacy recognized label for older archived reports;
- a complete RMS Charge customer directory for authorized staff, with account
  search, customer identity, balances, limits, status, recent charge activity,
  and direct Customer Profile access alongside the existing weekly account-list
  workflow. Weekly imports reuse exact linked RMS account numbers, then unique
  legacy Customer codes, before the existing unique-phone fallback;
- release-blocker repair for the RMS Charge customer directory and completed-sale
  void refunds: customer rows now open the full Customer Relationship Hub from
  the RMS workspace, the workspace tabs expose their real selected state to
  keyboard and assistive input, and a multi-tender void keeps its full original
  paid amount in the durable refund queue until every source is settled. Retrying
  a successful refund source remains idempotent and cannot close the obligation
  for a second cash, check, card, gift-card, or credit source;
- Register email collection that prompts when a loaded Customer has no email,
  distinguishes a load-local skip from a persistent customer decline, records
  staff/date attribution for saved addresses, and supplies daily and
  staff-level Email Collection reporting. New-customer intake keeps Email, SMS,
  operational SMS, and operational email choices visible while defaulting them
  to approved. Choosing Save email with an empty field remains visible guidance
  and does not manufacture an automated product-error incident;
- consistent natural merchandise variation ordering across Register selection,
  POS Inventory, Product Hub tables and printed Stock Reports, variation setup,
  global search, promotions, and storefront options. Apparel sizes follow their
  merchandise order, numeric sizes sort numerically, ordinary text sorts
  alphabetically, and equivalent slash/hyphen suffixes such as `M20001/2` and
  `M20001-2` remain adjacent without merging distinct SKUs;
- customer receipts that present each merchandise line's saved **4.75%**,
  **4.00%**, and **Total Tax** amounts together on one compact Epson Font B line,
  including visible zero components for clothing and non-taxable lines. Pickup and
  payment-on-order receipts replace competing oversized payment-status/history
  headings with one compact **Order payment** summary containing prior payment,
  today's tender, target Order, remaining balance, and paid-in-full state. Emailed
  receipt images now scale to the available message width instead of clipping the
  right edge, while printed items use whitespace and bold product names—without
  divider rules—to make the start of each item clear;
- cash overpayments that make **Change due** unmistakable on Sale Complete with
  a large high-contrast amount beneath Receipt Contact. The Payment ledger shows
  the complete cash **Payment**, **Paid to Sale Amount**, and **Change Due** from
  the saved tender metadata, while the customer receipt prints **Cash Tendered**
  and **Change**;
- Receipt Builder protection that permits only the intentional repeated
  Transaction/customer/date fields in the barcode stub, keeps unexpected
  duplicate tokens blocked, and enables print/email/text tests for valid
  templates. Test Email now sends the rendered receipt as an inline PNG so
  Outlook-compatible clients display the receipt rather than an empty SVG
  frame;
- Receipt Settings recovery that validates Standard and Picked Up templates
  against their separate production contracts. The Picked Up template no
  longer requires the legacy payment-history block that the production
  renderer intentionally suppresses, each template tab shows its own issue
  count, and one action restores the Riverside default. Digital delivery now
  shows live Store Email and Podium readiness with the existing status-pill
  pattern, including unsaved switches, missing setup, and provider failures.
  Test Text enforces the saved receipt-specific switch and sends the saved MMS
  caption with the current receipt image;
- restart-safe, background Podium contact reconciliation. Settings starts the
  audited comparison without holding the browser request open, polls persisted
  progress, and shows eligible, mapped, unsynchronized, queued, failed, and
  conflicting customer counts. Every cursor request retains Podium's 100-contact
  page size, unchanged successful contacts avoid redundant indexing and provider
  writes, and only missing or failed Riverside contacts enter the outbound
  queue. Main Hub startup closes an audit run interrupted by the previous server
  process, while a genuinely concurrent run remains single-instance and
  ambiguous identities remain blocked for safe review;
- Wedding Party **Style & Pricing** inventory lookup now requires every entered
  search term, so a hyphenated SKU cannot collapse to a broad prefix and show
  unrelated variants;
- RMS Charge account imports now match existing accounts through authoritative
  Customer identity when an external account identifier is absent, avoiding
  duplicate account creation while preserving explicit identifier matches;
- a printable **Stock Report** in each Inventory Product Hub that covers the
  open parent item and every variation, including zero-stock variations. It
  reports current on-hand quantity, the authoritative last positive sale date,
  complete-history average units per month, annualized average units per year,
  parent trailing-12-month sales ranking, and parent units sold during the last
  30 days. All sales measures come directly from qualifying non-cancelled
  Transaction lines rather than the Product Hub's limited recent-history
  display, and printing uses the configured Reports printer without changing
  inventory;
- customer merges that preserve the surviving Customer's usable contact and
  profile details while safely consolidating duplicate records;
- a contact-first Customer Hub Profile that puts name, phone, email, and address
  above compact operational summary and insight cards, uses denser desktop
  field grids, and reduces the height of the persistent Save Profile bar;
- a full-roster primary Salesperson menu in the Register header, with every
  active Salesperson visible without a separate scrollbar inside the menu;
- nationwide Geoapify address autocomplete across Customer, shipment, and POS
  shipping workflows. Selecting a result fills the available address fields
  directly without calling Shippo, municipality labels such as **Town of Eden**
  populate the City field as **Eden**, and opening an existing Customer Hub
  profile no longer starts a lookup until staff edit the street address;
- a simplified Podium account-authorization flow with clearer setup status and
  callback handling for staff. Non-loopback sessions consistently display
  `https://ros.riversidemens.com/callback`, and an internal-address session
  directs staff to open secure Riverside before authorization so the browser-bound
  setup session is preserved on the callback origin;
- simplified IONOS mailbox setup: staff enter one email address and password,
  and Riverside securely applies the same login to both incoming IMAP and
  outgoing SMTP credentials, including linked credential clearing;
- corrected partial-payment cancellation and exchange behavior: returned value
  now satisfies the unpaid balance before Riverside offers exchange credit or a
  customer refund, cancelled lines remain visible in the Transaction Record,
  and failed refund handoff remains visibly recoverable instead of closing the
  order detail; an audited preview-first support repair can offset an orphaned
  internal exchange credit without touching Helcim or inventory;
- corrected Register-close telemetry that distinguishes the actual database
  commit from total close-workflow duration, plus migration-backed
  consolidation of legacy per-run audit-probe alerts into one current incident
  without discarding historical probe runs;
- a documented Main Hub operations review covering seven days of checkout,
  refund-provider, Register-close, workstation, audit-probe, and Windows event
  evidence plus the last 48 hours of automated Bug Manager reports; the review
  found no new Riverside crash or server-side HTTP 500 and leaves negative
  Counterpoint inventory for physical reconciliation through normal stock
  workflows;
- Bug Manager diagnostic capture that removes customer contact fields, URL
  query values and fragments, credentials, and tokens before submission;
  hidden browser tabs no longer report false Main Hub outages, private-network
  heartbeat rejection stops the browser retry loop, and ordinary correction or
  decline guidance—including an empty customer-email collection field—uses
  warning/information severity instead of manufacturing application-error
  evidence;
- complete Inventory Reconciliation totals, validated issue filters, bounded
  paging, and full filtered CSV export without the former per-issue 100-row
  truncation. Existing negative availability remains queued for authoritative
  physical-inventory or Counterpoint reconciliation and is never repaired by a
  guessed stock write;
- installed Main Hub environment precedence over inherited scheduled-task
  values, truthful restart-required responses and staff guidance after
  Meilisearch credential changes, and bounded Geoapify address requests whose
  logs retain only safe failure categories;
- truthful Wedding Deposit-only receipts without fabricated merchandise,
  bounded percentage discounts, exclusion of alteration-service prices from
  discount audit findings, and normalized case-insensitive matching of
  historical Counterpoint gift-card applications;
- a direct, scrollable POS navigation rail with every permitted destination
  available without opening nested Work or More groups; the rail can still
  collapse to its icon-only form, while the hamburger remains the small-screen
  control that opens the full navigation;
- complete audience-specific dashboards for Register staff and Back Office
  operators. The POS Dashboard restores register identity, shift priorities,
  sales, notifications, pickups, alterations, tasks, inventory, Wedding Pulse,
  weather, and Sales by Hour, while Back Office operational detail opens fully
  by default and remains independently collapsible;
- consistent Back Office workspace headers and metric cards across Customers,
  Orders, Inventory, Loyalty, Gift Cards, and related management surfaces, using
  the shared full-width workspace contract without changing domain actions;
- a searchable Settings Hub with task-based, independently collapsible groups.
  Direct provider and device pages remain the owning configuration surfaces,
  deep links remain supported, and the former duplicate Integrations Overview
  route returns staff to the Hub instead of creating a second settings owner;
- an action-first Register toolbar with **Customer Orders** first, **Custom
  Order** in **More Actions**, and transient More behavior that closes before
  the selected workflow opens;
- a compact Payment deposit strip that reports green **DEPOSIT MET**, red
  **NEED 25% DEPOSIT**, or red **NO DEPOSIT**, with Manager Access available
  inline for an audited below-minimum exception;
- focused POS Customer and Order surfaces that keep lookup and fulfillment
  actions first, request 25 Customer rows instead of the Back Office management
  payload, and leave bulk/profile-quality and advanced reporting controls in
  Back Office;
- one shared notification preview owner for the top bar and dashboards,
  semantic duplicate suppression in the morning priority queue, and removal of
  duplicate Register-side notification and offline-queue polling;
- indexed Customer browse ordering through the existing customer-name index;
  the measured page-candidate query dropped from approximately 419 ms to 2.8 ms
  on the local 26,628-Customer development dataset;
- a pinned variation-pricing keypad and persistent Item to Build context so
  staff can review or correct exact choices without losing the active price
  step;
- a visible **Cancel Item** action in Register **Customer Orders → View Order
  Details**, directly alongside the selected line's update controls, for
  audited cancellation of unfulfilled Special, Custom, Wedding, and Layaway
  lines;
- cancellation recalculation that applies the removed value to the remaining
  Transaction balance first and sends only genuine overpayment into the
  existing multi-source refund flow, including Helcim refund-ledger
  verification before **Record Sale** unlocks;
- inventory-safe cancellation: Ready-for-Pickup items remain on hand while
  their reservation is released, Layaway holds are released, future receiving
  excludes fully cancelled quantities, and Wedding progress excludes cancelled
  lines;
- complete Customer receipt-event history and direct reprint for sales,
  deposits/payments, pickups, pickup-with-payment, returns, and exchanges,
  including source Fulfillment Order numbers on multi-order pickup receipts;
- completed-sale receipt lookup in Return / Exchange, so fulfilled merchandise
  remains discoverable after its original Transaction closes;
- multi-source refunds and exchange remainders that retain every selected
  payment method under one auditable return event, resume safely after a
  partial processor failure, and allow return credit to fund an existing
  Transaction payment without the cart silently removing that payment;
- Register wedding-member handling that keeps the quantity/price pinpad at its
  full touch size, removes duplicate member-name presentation, and uses compact
  party rows with party, role, date, status, **Start Order**, and **Measure**;
- explicit party selection when a customer belongs to multiple active weddings,
  with the Wedding Checklist and Wedding Hub handoff bound to the selected party
  and party switching blocked once Cart work exists;
- payer-scoped Wedding Deposit history with receipt review/reprint, resume of
  funded unposted orders, and **Build More Items** for a new member Wedding Order
  without rewriting earlier deposits, Transactions, or receipts;
- Wedding Builder variation discounts preserved through member checkout and
  backdated booking events aligned to the Manager-approved business date;
- one integrated Orders pickup and payment workflow: after staff chooses the
  items being picked up, the Register shows the authoritative balance and asks
  whether to pay it now or continue without payment. Staff can collect full or
  partial payment in the same pickup flow; skipping payment requires Manager
  Access when the required deposit has not been met. A fully picked-up
  Transaction remains **Open** and payable while money is still due;
- release validation aligned with the direct POS navigation and order handoff:
  cancellation refunds continue into Register Pay, and returning to Orders
  reopens the authoritative Order detail without an intermediate navigation
  group;
- staff-facing **Wedding Order** labels in Daily Sales/CSV output and Wedding
  Order receipts that identify the party name and wedding date;
- matching on-screen Daily Sales, Daily Sales print/preview, and Z-Report tender
  totals sourced from the authoritative payment ledger. Retained Wedding
  Deposits remain informational and no longer reduce the card tender total;
- matching Daily Sales and Z-Report day metrics for RMS, cash, deposits, tax,
  shipping, alterations, gift-card loads, and discount counts. Discount audit
  comparisons use each Transaction line's recorded original price rather than a
  later catalog price;
- a Helcim final-save guard that remains **Not Ready / Waiting for Card** until
  the current checkout receives a confirmed final provider outcome, plus a
  two-minute customer card/PIN window before recovery guidance replaces the
  normal waiting state;
- refund-only **Original Card** settlement on **Apply Refund**, with **Record
  Sale** unavailable until ROS verifies approved/captured Helcim status, the
  provider refund ID, and the committed refund-ledger event. Multi-source
  refund retries reuse stable tender identities, while exchange card remainders
  remain deferred until replacement and inventory linkage are recorded;
- corrected multilingual Kokoro/SenseVoice certification for English by using
  an ordinary-English fixture, explicitly selecting SenseVoice English with
  inverse text normalization, and retaining deterministic bounded subsystem
  diagnostics without exposing the speech loopback in the updater window;
- nonblocking ROSIE certification: a degraded local intelligence subsystem is
  reported and retried without failing or rolling back an otherwise healthy
  Riverside update, while staff receive concise recovery guidance;
- authoritative Wedding Hub lifecycle status derived from each member's
  Transaction/Fulfillment Order lines instead of editable compatibility flags;
- live Wedding Hub aggregation of linked Customer contact data, purchased
  Transaction-line merchandise, applied payments, remaining held deposits,
  current balances, alterations, and party-scoped appointments, refreshed on
  source events, focus, and a one-minute open-party interval;
- payment labels that keep unredeemed held funds as **Deposit**, show applied
  money with an open balance as **Partial**, and require a zero-balance linked
  Transaction for **Paid**;
- catalog-backed party setup for suit, shirt, tie, shoe, and accessory parent
  products with **All**, **Groom Only**, **Groomsmen Only**, **Any**, and
  role-specific **Other** rules applied before member variation selection;
- authenticated, atomic party/member/appointment activity logging and exact
  Transaction handoff from Wedding Manager into Orders;
- schedule-conflict overrides that retain the selected staff ID, require
  Manager Access and a written reason, and atomically complete linked
  Measurement/Fitting milestones when attendance is recorded;
- Manager **Archive Tracking** for completed-outside-ROS, cancelled,
  incomplete, legacy, duplicate, or test party trackers. Archiving records the outcome,
  reason, notes, actor, and open-work snapshot without fabricating fulfillment
  or changing Transactions, balances, held deposits, appointments,
  alterations, shipments, or customer history; closed weddings remain
  searchable under **Closed / Archived** and can be reopened with retained
  audit history;
- compatible PostgreSQL client-tool selection for local development;
- strict GitHub SHA-256 verification before Main Hub ZIP extraction;
- exact-version handling so an older release cannot appear as a same-version
  rebuild merely because its build SHA differs;
- complete Main Hub ROSIE pin application with rollback assets retained until
  server readiness succeeds;
- efficient ROSIE runtime/STT/TTS reuse: matching pins remain in place, while
  changed, missing, or corrupt assets alone are replaced and certified;
- a release gate binding the packaged llama.cpp runtime version and SHA-256 to
  the installer pin.

## Wedding deposits and member order building

- **Collect & Build** prepares party parent products, exact member variations,
  optional alterations, individual No Tax reasons, and apply-all or per-member
  salesperson attribution as reloadable drafts.
- Approved payer funding unlocks **Create All Member Transactions**. Each
  beneficiary receives a separate financial Transaction and Wedding
  Fulfillment Order through the normal server-validated atomic checkout path.
- Stable checkout identities make partial retry safe: completed members remain
  posted once and a retry resumes only unfinished members.
- Deposit entry supports one per-member amount applied only to selected party
  members, with individual overrides still available.
- Deposit Only activity can be resumed from the original payer's **Orders &
  Receipts** history, which reloads member allocations, exact held balances,
  posted Transactions, and receipt links.
- Selecting a wedding member directly in Register can open the same parent-item
  and exact-variation builder while identifying available held funds and the
  contributing payer.
- Builder navigation, draft editing, and item selection remain nonfinancial.
  Writes happen only through the existing audited checkout service.
- Completion presents member totals and individual printable receipts.

## Wedding lifecycle, scheduling, and tracking archive

- Ordered, Received, Ready for Pickup, and Picked Up are derived from the
  authoritative non-takeaway Transaction/Fulfillment Order line lifecycle.
  The Wedding grid no longer creates a parallel fulfillment state.
- Pickup appointments can be marked attended, but merchandise pickup must be
  completed through Orders/Register so inventory, tender, fulfillment,
  alterations, shipping, and receipt links stay intact.
- Linked Measurement/Fitting attendance and the matching member milestone are
  one atomic write. Appointment conflict overrides retain the exact ROS staff
  assignment, Manager Access identity, validation message, and written reason.
- Party/member mutations use authenticated ROS staff attribution, and their
  activity entries commit with the business update.
- Recent member financial activity can open its exact Transaction in Orders,
  where the linked fulfillment, alteration, shipment, payment, and receipt
  evidence remains available.
- Passed or historically incomplete wedding tracking can be archived without completing
  a fictional ROS workflow. The read-only archive review discloses Transactions, open
  balances, held deposits, fulfillment lines, appointments, and alterations;
  unresolved work requires explicit acknowledgment and remains unchanged.

## Product variation navigation

- Cart additions, existing-line changes, Wedding Builder selections, and
  Customer Orders item updates retain a visible **Item to Build** summary.
- **Back** remains available at every choice and on pricing review.
- Staff can select a completed choice to edit from that point without
  restarting the item or losing earlier selections.
- Focused Playwright coverage protects the shared selection contract.

## Main Hub audit and recovery

- Main Hub environment generation now safely serializes Windows paths and
  settings-owned values for both the Rust dotenv loader and packaged PowerShell
  consumers. Updates also require the prior server process and executable handle
  to be fully released before replacement, preventing the two attended recovery
  conditions found during physical Main Hub installation.
- The Windows system audit now follows the actual production installation and
  reports credential-key posture without relying on retired deployment config.
- Credential-key repair uses the production server environment contract and
  keeps secrets out of logs and generated evidence.
- The deployment launcher and example configuration no longer advertise the
  retired credential-key config input.
- The incident review remains historical operational evidence; publication of
  v0.96.0 does not by itself prove that a physical Main Hub installed it.

## Help, staff documentation, and ROSIE

- POS, Wedding Manager, order-load, receipt-summary, and staff wedding manuals
  describe the current live-source tracker, archive, party item audiences,
  builder, funding, variation, retry, and receipt paths.
- Deposit, Transaction/Wedding Fulfillment Order, group-pay, UI convention,
  Counterpoint cutover, and Windows installer docs are aligned with the shipped
  behavior.
- ROSIE and AI-assisted Help use policy pack `rosie-policy-pack-0.96.0` and the
  pinned official Gemma 4 E4B QAT Q4_0 text/vision pair. Answer streaming,
  usage telemetry, request-scoped image attachments, permission-gated native
  read-only tools, readiness certification, failed-upgrade rollback, and
  cleanup of superseded managed models are included. Public cloud providers
  remain disabled.
- Kokoro speech defaults to **ROSIE Maple (US English)**, with **ROSIE Sol (US
  English)** and **ROSIE Vale (British English)** as the other supported
  choices. Unsupported or older saved speaker numbers fall back to Maple in
  both the server and desktop runtime.
- Approved Help manuals, staff corpus files, and ROSIE policy documents are
  compiled into the server for packaged Main Hub operation. Settings reports
  the bundled intelligence pack without requiring a development checkout;
  source-regeneration and optional Meilisearch reindex controls remain limited
  to installations that actually include those build capabilities.
  `AI_CONTEXT_FOR_ASSISTANTS.md`, `AI_REPORTING_DATA_CATALOG.md`,
  `PLAN_LOCAL_LLM_HELP.md`, and `ROSIE_OPERATING_CONTRACT.md` remain a reviewed
  bundle. Daily Financial Report headline and month-to-date figures now follow
  canonical booked Daily Sales while fulfillment-recognized revenue remains a
  separately labeled detail section.

## Input-method accessibility

- Staff-facing cards and rows that behave like controls expose consistent
  focus, keyboard activation, and semantic button behavior without changing
  their underlying domain actions.
- Search, notifications, customer, payment, inventory, staff, scheduler,
  wedding, and POS workflows now preserve visible focus and work across mouse,
  touch, keyboard, and assistive input.
- Dedicated Playwright coverage protects representative Enter/Space activation,
  focus visibility, and interactive nesting contracts.

## Validation and publication contract

Release publication requires all of the following on one exact commit:

- version parity across root, client, server, Tauri, ROS Dev Center, Deployment
  Manager, Server Manager, CounterSync Bridge, locks, and Windows metadata;
- generated Help and ROSIE knowledge-drift checks;
- Rust formatting, clippy/checks, unit/integration tests, client lint,
  typecheck, production builds, and the non-E2E local release gates;
- local Playwright is explicitly skipped for this same-version rebuild at the
  release owner's direction; exact-commit GitHub Playwright remains required;
- exact-commit GitHub Lint and Playwright gates;
- successful non-publishing Windows and macOS candidates;
- an annotated `v0.96.0` tag on the candidate commit;
- candidate promotion with artifact provenance verification;
- one current Windows deployment ZIP plus five matching updater/build-manifest
  pairs, all resolving to the release commit;
- a public, non-prerelease GitHub release marked Latest.

## Operational boundary

Source, CI, tag, GitHub release, packaged assets, deployment, and live runtime
are separate claims. GitHub publication does not prove installation. Store
signoff still requires the physical Main Hub and Register estate to report
v0.96.0 and the exact build SHA, pass `/api/ready` and `/api/health`, show the
expected migration ledger, and complete printer, payment-terminal, backup, and
representative Wedding Builder smoke checks.
