# Riverside OS v0.96.0 Certification Evidence

- Status: **Candidate preparation in progress; only checked evidence is
  certified**
- Version: **v0.96.0**
- Prepared: **2026-08-16 recovery-integrity, split-refund, complete Playwright gate, build-provenance, Customer Hub, Podium status, Register, and search-recovery rebuild**
- Baseline tag: `v0.95.5`
- Release tag: `v0.96.0`

## Source identity

- [x] Release preparation is committed on `main`.
- [ ] Local `HEAD`, remote `main`, annotated tag `v0.96.0`, GitHub release
  target, candidate run SHAs, and updater build manifests identify one exact
  full commit.
- [x] Root, client, server, Tauri, companion apps, ROS Dev Center, lockfiles,
  and Windows deployment metadata declare `0.96.0` in the release-preparation
  worktree.
- [x] The original source delta is the six commits after v0.95.5 plus the
  v0.96.0 release-preparation commit; subsequent same-version rebuild changes
  are recorded in the release notes and changelog.

## Local validation

For the final 2026-08-16 recovery-integrity candidate, checkout and receipt
recovery payloads that claim a Transaction missing from the current Main Hub
are retained as blocked evidence without an invalid foreign-key link. The
original claimed identity remains in the recovery payload and the evidence can
still follow the normal Manager-reviewed dismissal path. Rust formatting and
compilation, client lint/typecheck, Help impact, whitespace, and the complete
non-E2E pre-retag gate must pass before tagging. Per the release owner's
direction, the new server-backed recovery contract is exercised only by the
exact-commit GitHub Playwright suite, not by a local E2E run.

For the final 2026-08-16 build-provenance candidate, required GitHub
Playwright shards allow up to 60 minutes for the complete standard suite, and
the local server launcher supplies the current commit as an explicit Cargo
build input so non-Rust commits refresh the embedded build SHA. Shell syntax,
Rust formatting, and the complete non-E2E pre-retag gate must pass before the
candidate is tagged. Per the release owner's direction, no local Playwright
E2E run is performed; exact-commit GitHub Playwright remains required.

For the final 2026-08-16 reporting and release-gate candidate, split refund
sources remain one booked return/refund event with every tender component
retained, preventing valid multi-tender refunds from creating duplicate Daily
Sales identities that block Z-close. GitHub's required Playwright workflow now
runs the complete standard suite across four shards instead of a maintained
allowlist; the release owner's no-local-E2E direction remains in force, so the
new exact-commit GitHub run is the required complete-suite proof. Rust
formatting, the focused split-refund unit regression, client typecheck, full
client lint, Help impact, and whitespace checks passed before commit.

For the current 2026-08-16 Customer Hub and Podium status candidate, Customer
Profile uses the previously open space beneath contact details for Quick look
cards, and Customer History groups same-second activity without merging or
discarding any underlying event or receipt evidence. Podium Inbox treats an
active history pull as progress and raises attention only if history remains
incomplete after the pull stops or another independent provider-health problem
exists. The release owner explicitly directed that no local Playwright E2E run
be performed for this rebuild. Help generation reported 73 approved manuals and
0 drafts; client typecheck, full client lint, Help impact, AI knowledge drift,
and whitespace checks passed. Exact-commit GitHub Playwright remains a blocking
release requirement alongside Lint, Windows, macOS, and updater-manifest proof.

For the 2026-08-16 candidate, Register preserves its authoritative checkout,
payment, inventory, permission, and recovery paths while improving the staff
hot path. Parked work remains directly visible, exact server-verified SKU and
barcode-alias scans add immediately, camera teardown is deterministic, and
Cart-owned dialogs receive focus containment, safe Escape handling, and focus
restoration. Below 1024 px, staff explicitly switch between Cart and
**Customer & Pay**; the desktop dual-pane layout is unchanged. The same
candidate carries the system-search and Meilisearch health/recovery hardening
and documents both the full Register audit and open-source enhancement roadmap.
Focused validation passed at phone, tablet, iPad, and desktop sizes, including
16 targeted runtime journeys, five scanner/dialog/checkout source contracts,
client typecheck, lint, production build, Help impact, and whitespace checks.
The complete non-E2E pre-retag gate also passed, including all 84 go-live
blockers, dirty-database migration rehearsal, migration layout/embedding, Rust
formatting, and migration parser tests. Publication still requires exact-SHA
GitHub Lint, Playwright, Windows, macOS, and updater-manifest proof.

For the 2026-08-15 candidate, Customer Relationship Hub uses the same
directional, day-grouped presentation as Podium Inbox and combines linked text
messages and deduplicated Podium call lifecycle events under **Text & calls**.
The same customer calls appear in Customer History through a permission-checked
customer endpoint. The candidate also includes component-aware Main Hub update
packages and conservative fail-closed offline checkout recovery. Client
typecheck, scoped lint, Rust formatting and compilation, focused Podium call
parser tests, Help impact, the static Customer Hub source contract, whitespace
validation, and the complete non-E2E pre-retag gate passed. The pre-retag gate
included all 84 go-live blockers, dirty-database migration rehearsal, migration
layout/embedding, migration parser tests, client typecheck, and client lint.
The release owner explicitly directed that no new local Playwright E2E run be
performed; exact-commit GitHub Playwright remains a blocking publication gate.

For the final 2026-08-13 historical-sales rebuild, Counterpoint tickets that
contain only lifecycle `U` rows are no longer retained as booked sales. A
source rerun marks the legacy initial booking event with the governed
`counterpoint_order_fulfillment` reporting exclusion while preserving the
Transaction, lines, payments, returns, fulfillment state, and audit history.
The focused lifecycle-only database regression and Counterpoint financial-line
classification test passed, along with Rust formatting, Help impact, and
whitespace checks. The operator explicitly waived a local Playwright E2E run;
publication requires the final exact-commit GitHub Playwright workflow to pass.

For the 2026-08-13 Podium call-delivery and Inbox clarity rebuild, the
candidate recognizes nested call webhook payloads, exposes the newest call in
conversation summaries, and reports the stored-call count and latest received
call so a missing provider subscription is explicit. Unmatched conversations
use their phone number as the primary identity, and the Podium Inbox uses blue
for its selected state, primary actions, and Riverside outbound messages.
Focused Rust call-parser tests and Podium Playwright source contracts passed
during implementation; the clean exact commit must pass the complete pre-retag
gate and release Playwright coverage before publication evidence is certified.
The release Playwright run also exposed a mobile-navigation helper racing the
sidebar transition before opening Settings. The helper now activates mobile
rail controls through their native button action after opening the menu; its
phone, tablet, iPad, and desktop workspace cases passed together before the
complete exact-commit rerun.
Bug Manager diagnostic-download coverage now follows the current Settings Hub
category path through **Help & System** instead of relying on the retired flat
Settings sub-navigation locator. It then follows the consolidated **Advanced
Diagnostics → Bug & Error Diagnostics** path, waits for the lazy Bug reports
workspace, and uses the current **Automated diagnostics** control. The focused
six-case diagnostics spec passed together, including a current notification
health fixture and proof that Workstations remains usable when the Operations
health snapshot feed returns `503`.
Help Center Manager and ROSIE Settings coverage now follows the same current
Settings Hub category contract through **Help & System**, rather than searching
the retired flat sidebar for those panels.

On 2026-08-13, the complete local catalog was stopped at its first failure as
required after `143` passes and `5` conditional skips exposed that retired Help
Center locator. The corrected Help Center Manager and ROSIE Settings set then
passed all `5` focused cases. The operator explicitly waived another complete
local E2E rerun after that focused proof; a subsequently started rerun was
stopped during database bootstrap before any test began. Publication therefore
requires the final exact-commit GitHub Playwright workflow to pass.

The same candidate includes controlled Counterpoint one-shot repair filters,
audited passed-preflight reuse for an immediate retry, and preserved underlying
Main Hub network failure details. The bridge rejects unknown or disabled entity
filters before starting a write pass, while ROS remains responsible for
revalidating a reused preflight UUID when the new import run starts. Payment-
only duplicate matching now returns a Transaction only for one exact candidate;
its focused Rust regression covers zero, one, and multiple candidates. Historical
source replacement also permits Counterpoint's own reviewed paid-price repair and
reconciliation audit events while continuing to block staff return, refund,
exchange, payment-allocation, fulfillment, procurement, and line-audit activity.

For the 2026-08-12 Operations, Podium, QBO, and payment-recovery rebuild, the
operator explicitly waived a new local Playwright E2E run. The candidate adds
the simplified Operations workspace, linked-staff Podium assignment and
PIN-verified responder selection, call/review lifecycle activity, optimized
background contact reconciliation, the ROS-to-QBO GL crosswalk, and guarded
release of an unresolved Card Not Present attempt while retaining provider
evidence. Implementation included focused source-contract coverage, client
typecheck/lint, Rust formatting and compilation, Podium Rust tests, Help impact,
migration-layout validation, and whitespace validation. The final clean commit
must still pass the complete non-E2E pre-retag gate. Publication relies on
exact-commit GitHub Lint and blocking Playwright plus successful Windows and
macOS packaging and updater/build-manifest verification.

For the 2026-08-10 mailbox, Podium history, and appointment hardening rebuild,
the operator explicitly waived a new full local Playwright release-suite run.
Before candidate preparation, the repair-focused 41-test local Playwright set
passed with one worker, including mailbox triage, truthful Podium history,
unknown senders in the regular Inbox, appointment deep links, all four scheduler
viewports, staff scheduling, and Operations Center routing. Client lint,
typecheck, the production build, Help quality/impact review, and whitespace
validation also passed. The clean candidate must still pass the repository's
complete non-E2E pre-retag gate; publication relies on the exact-commit GitHub
Lint, blocking Playwright, Windows, macOS, promotion, and updater/build-manifest
evidence.

For the 2026-08-09 same-version rebuild, implementation validation completed
before the release request: 835 Rust library tests passed with 14 explicitly
environment-gated tests ignored; the 204-test blocking Playwright catalog
passed; and the complete 590-test Playwright catalog produced 574 passes, 15
conditional skips, and one stale Podium Settings assertion. That assertion was
corrected and its focused workflow then passed in a fresh isolated environment.
Client lint/typecheck, production build, Help generation/impact, deployment
release validation, the 84-gate go-live contract, Windows install-contract
validation, migration rehearsal, Rust formatting, and whitespace validation
also passed. The operator waived another local E2E repetition after the final
documentation commit; publication relies on the required exact-commit GitHub
Playwright and packaging workflows. The guarded pre-retag gate must still pass
on the final candidate commit.

- [x] `cargo fmt --all --check`
- [x] `cargo clippy --workspace --all-targets -- -D warnings`
- [x] Rust library tests (`835 passed`, `14` explicitly environment-gated
  tests ignored, `0 failed`)
- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] production builds for the main client, ROS Dev Center, Deployment
  Manager, Server Manager, and CounterSync Bridge GUI
- [x] `npm run generate:help` (73 approved manuals, 0 draft)
- [x] `npm run check:help-impact`
- [x] `python3 scripts/verify_ai_knowledge_drift.py` (114 corpus paths)
- [x] `npm run check:pre-retag` (including 84 go-live blockers and dirty
  existing-database migration rehearsal)
- [x] `git diff --check`
- [x] blocking local Playwright E2E with one worker (`204 passed`, `0 failed`)
- [x] complete local Playwright catalog with one worker (`574 passed`, `15`
  conditional skips, one stale Podium assertion corrected and passed in its
  focused rerun; 590 discovered)

## Exact-commit GitHub evidence

- [ ] Lint Checks passed on the release commit.
- [ ] Playwright E2E shards and aggregate gate passed on the release commit.
- [ ] Windows full-deployment candidate passed without publishing.
- [ ] macOS ROS Dev Center candidate passed without publishing.
- [ ] Candidate provenance validation confirmed both runs built the tagged
  release commit.
- [ ] Promotion completed successfully.

## Release and asset evidence

- [ ] `v0.96.0` is an annotated tag on the exact release commit.
- [ ] The GitHub release is public, non-prerelease, and Latest.
- [ ] The title and body identify the exact build SHA and promotion workflow.
- [ ] Exactly one `RiversideOS-v0.96.0-<sha>-Windows-Deployment.zip` is present.
- [ ] Windows Register, Deployment Manager, Server Manager, CounterSync Bridge,
  and macOS ROS Dev Center updater manifests reference the exact release commit
  and matching non-empty artifacts/signatures.
- [ ] All five build manifests reference the exact release commit.
- [ ] No stale or cross-version replacement assets remain.

## Functional release evidence

- Operations contract coverage protects the prioritized Today workspace,
  actionable versus informational diagnostic grouping, live readiness proof,
  and access to advanced support evidence.
- Podium contract coverage protects assignment to linked active staff without a
  reply, a responder roster restricted to active Podium-linked staff,
  server-enforced PIN-verified remembered responders, call and review webhook
  activity, published-review follow-up, background reconciliation status,
  complete 100-contact pagination, and collision-safe customer matching.
- QBO crosswalk coverage protects the full Riverside GL catalog, server-side
  validation of both account systems, visible match/review state, and the rule
  that the QBO account remains the posting identity.
- Card Not Present isolation coverage protects the explicit unresolved-attempt
  release, retained provider evidence, later exact-approval supersession, and
  the prohibition against treating an unknown provider result as a decline.
- Receipt renderer coverage verifies whitespace rather than divider rules
  between consecutive items while keeping product names emphasized; delivery
  contract coverage protects the responsive inline receipt image and current
  receipt email subject contract.
- Podium setup contract coverage protects the public production callback,
  secure-origin action, browser-origin readiness gate, encrypted app-key setup,
  and OAuth approval flow.
- Variation-selection E2E covers persistent Item to Build context, Back
  navigation, and editing completed choices.
- Wedding Deposit/Cart E2E covers payer funding, selected-member amounts,
  review behavior, and financial preflight.
- Wedding order batch E2E covers separate member Transactions, Wedding
  Fulfillment Orders, stable retry identities, exact held-deposit use, and
  individual receipt results.
- Wedding integration coverage protects authoritative fulfillment-derived
  status, schedule override attribution, atomic appointment milestones, modal
  layering, and exact Orders handoff.
- Wedding tracking-archive API coverage creates a historical party, detects open work,
  rejects an unacknowledged archive, records the manager action, verifies
  archived outcome metadata, and reopens the party without changing the linked
  records.
- Main Hub audit script validation covers production config discovery and
  credential-key posture without exposing credential material.
- Error-fix regression coverage protects installed Main Hub environment
  precedence, Meilisearch restart-required responses, bounded/redacted
  Geoapify failures, private Bug Manager diagnostic capture, hidden-tab
  connection stability, and alteration-service discount-audit exclusion.
- Inventory Reconciliation API and UI coverage verifies complete store-wide
  counts, validated filtering, bounded pages, and complete-queue export.
- Counterpoint import coverage verifies normalized gift-card application
  matching, while receipt coverage verifies deposit-only Wedding activity
  without fabricated merchandise lines.
- Windows deployment validation requires the Cube role helper, its database
  setup call, migration 185 routing, transaction-scoped `SET ROLE`, temporary
  authority cleanup, and successful migration message as one contract. Package
  assembly must install the pinned Cube dependencies on Windows, include the
  Windows-native binding and portable Node host, force loopback binding, and
  require `/readyz` before the Main Hub update can complete.
- Help/manual generation and ROSIE knowledge-drift validation certify the
  staff-facing and AI-grounding corpus bundled with the release.
- Native Insights validation protects ROSIE report-spec validation, the
  signed loopback Cube request, approved semantic members, Admin-only
  cost/margin controls, and Cube's `cube_ro`-only access to governed
  `reporting.*` views in the shared Riverside PostgreSQL database.
- Podium settings and server tests protect independent workflow enablement for
  staff texts, text receipts, fulfillment and alteration notices, appointment
  messages, and unknown-sender welcome messages while retaining legacy aggregate
  configuration compatibility.
- Packaged-runtime tests verify that approved Help, staff, and policy markdown
  remains available without a development checkout. Kokoro voice contracts
  bind the UI, server, desktop runtime, and certification probe to supported
  English speaker IDs and reject obsolete multilingual speaker selections.
- Migration 185 installation uses transaction-scoped temporary role authority,
  restores the application role's prior attributes before commit, and preserves
  rollback safety for both reporting schema and privilege changes.

## Operational boundary

This document certifies source and publication evidence only where explicitly
checked. Production installation remains a separate gate. Final store signoff
must verify the installed v0.96.0 version and exact SHA, `/api/ready`,
`/api/health`, database/migration state, Register connectivity, printers,
payment terminals, backup/restore evidence, and representative wedding deposit
and member-order workflows on the physical environment.
