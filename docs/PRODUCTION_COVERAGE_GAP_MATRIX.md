# Production Coverage Gap Matrix

This matrix tracks release-critical coverage for the Hybrid Tauri Host production path.

| Area | Existing coverage | Gap | Release action |
|---|---|---|---|
| Checkout idempotency | Server unique index and replay handling; E2E tender/RMS helpers use `checkout_client_id`; `client/e2e/offline-recovery-contract.spec.ts` covers blocked 4xx replay retention | Success dequeue after a restored network path remains best suited for a future client unit harness | Keep offline recovery contract in release evidence and add success-dequeue unit coverage when a client test harness exists |
| Split tender and order payments | API tests in POS alteration/order payment paths; tender matrix; `client/e2e/checkout-tender-financial-contract.spec.ts` directly asserts payment allocation rows across current sale + existing transaction balance | No remaining automated split/allocation contract gap identified in this audit slice | Keep checkout tender financial contract in `npm run test:e2e:tender` and release evidence |
| Check tender | Migration `138_payment_check_number.sql`; checkout stores check number; checkout now rejects missing check numbers; `client/e2e/checkout-tender-financial-contract.spec.ts` covers failed check checkout | No remaining automated check-number contract gap identified in this audit slice | Keep checkout tender financial contract in `npm run test:e2e:tender` and release evidence |
| Cash rounding | QBO journal mapping code and docs; transaction total recalculation includes `rounding_adjustment`; `client/e2e/checkout-tender-financial-contract.spec.ts` covers rounded cash checkout artifacts and QBO rounding impact | No remaining automated cash-rounding contract gap identified in this audit slice | Keep checkout tender financial contract in `npm run test:e2e:tender` and release evidence |
| Tax under `$110` and non-taxable lines | Rust tax unit tests; high-risk tax audit endpoint; tax-exempt UI test is back in CI via explicit POS readiness contracts; `client/e2e/tax-audit-contract.spec.ts` covers category inheritance, parent-product override, `$109.99`, `$110.00`, discount crossing, stale client tax rejection, tax-exempt reason enforcement, gift-card non-taxability, shipping no-tax treatment, return tax reversal, and QBO tax liability mapping | No remaining automated tax contract gap identified in this audit slice | Keep tax audit contract in release evidence and re-run after tax/category changes |
| Register close | `register-close-reconciliation.spec.ts` covers discrepancy/group behavior; `client/e2e/offline-recovery-contract.spec.ts` covers close blocking with pending/blocked checkout queue; close modal no longer falls back to seeded `1234` for reconciliation | Missing Staff Access blocker needs UI automation | Add Staff Access blocker test |
| Parked-sale close cleanup | Server-backed parked sales; Z-close now purges parked rows inside the close DB transaction and writes per-sale audit rows; `client/e2e/register-audit-contract.spec.ts` covers parked-sale purge/audit rows during close | No remaining automated parked-sale close-cleanup contract gap identified in this audit slice | Keep register audit contract in release evidence |
| Inventory receiving | `inventory-receiving-api.spec.ts` covers staging, exact-once receipt, direct invoice; `client/e2e/inventory-audit-contract.spec.ts` covers order-style checkout no stock decrement, pickup decrement, duplicate PO receipt retry, and return/restock refund queue truth | Hybrid host live receiving/pickup rehearsal still required | Attach inventory drill evidence from the deployment host |
| Physical inventory | `inventory-physical-ui.spec.ts` covers review/publish; session/list reads include reserved/layaway exclusion flags; full-scope snapshot parameter binding is fixed; local restore drill verified restored `product_variants` count | RC/production negative-stock rows still require reconciliation or written waiver | Attach inventory reconciliation signoff after SQL probes return zero unexplained rows |
| Commission | Logic for specificity, finalization, attribution guard; intelligence finance tests cover trace rationale; `client/e2e/commission-audit-contract.spec.ts` covers fulfillment timing, specificity order, finalized payout immutability, and internal SPIFF receipt exclusion | No remaining automated commission contract gap identified in this audit slice | Keep commission audit contract in release evidence and re-run after payout/rate logic changes |
| QBO staging | `qbo-staging.spec.ts`; proposal warnings and balanced totals; approval/sync now block unbalanced totals and unbalanced postable lines; `client/e2e/qbo-audit-contract.spec.ts` covers real checkout proposal balance, account mappings, pending dedupe, staging list, drilldown tender linkage, one-time approval, and store-local business-date cutoff | Live QBO sandbox/company sync drill still required | Attach accounting drill evidence for approve/sync/retry against intended QBO company or sandbox |
| QBO business date | QBO proposal and drilldown date cuts now use `reporting.effective_store_timezone()` and proposal payload includes `business_timezone`; `client/e2e/qbo-audit-contract.spec.ts` proves near-midnight UTC activity lands on the intended store-local business date | No remaining automated business-date contract gap identified in this audit slice | Keep QBO audit contract in release evidence |
| QBO tokens | Masked credential reads; strict production and credential activation now require a non-default token key; new token writes use `v2:` AEAD wrapping | Need integration smoke against an existing legacy encrypted row during key rotation rehearsal | Add operational key-rotation drill evidence |
| Backup/restore | Backup manager and health notifications; docs; restore now requires exact filename confirmation, listed backup path, no open/reconciling registers, pre-restore backup, non-production/emergency guard, and explicit strict-production `RIVERSIDE_BACKUP_DIR`; local non-production restore drill passed on 2026-04-25; restore preflight helper tests cover confirmation, strict-production lock, register blocker, and catalog membership | Hybrid Tauri host restore rehearsal still required | Attach host restore drill signoff |
| Hybrid host | Tauri build workflow and unified engine docs | Needs live host restart/drill evidence | Manual signoff required before go-live |
| SQL audit probes | `scripts/production_audit_probes.sql` read-only P0/P1 probes for money, stock, tax, commission, QBO, register, and backup invariants; local run captured at `docs/reviews/evidence/production_audit_probes_local_2026-04-25.txt` | Local run found 51 negative available stock rows after excluding explicit POS service/meta SKUs; release/production run still required | Reconcile or explicitly waive every negative stock row in RC/production, then run against restored database and production preflight snapshot |
| POS UI | POS shell, register panel, cart shell, and cashier overlay expose explicit Playwright readiness contracts; formerly quarantined POS UI specs run locally again without `ROS_QUARANTINE_UNSTABLE_POS_E2E`; RMS Charge browser helper now uses same-origin API proxy; deterministic non-admin RBAC checks now run instead of skipping; full release E2E reported 181 passed / 7 skipped / 0 failed on 2026-04-25 | Needs repeated CI evidence after merge; remaining skips are 5 opt-in visual baselines, 1 switch-staff path hidden by open till state, and 1 optional data-dependent commission diagnostic | Watch the next release-suite run and keep traces on any failure |

## Minimum New Tests Before Retail Deployment

- QBO unbalanced staging cannot approve or sync in endpoint-level coverage.
- QBO token key rotation drill confirms existing rows decrypt only with the configured key and new writes use `v2:` wrapping.
- Hybrid Tauri host restore drill proves backup restore boots the packaged app and leaves migration ledgers intact.
