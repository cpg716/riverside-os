# Legacy pre-launch migration history

These files are retained for audit/reference only after the schema-contract baseline reset.
They are not active migrations and are intentionally outside the root `migrations/*.sql` runner glob.
Fresh installs use the root `001` through `008` baseline migrations plus explicit seed scripts.
